# AUD-EXTREME-01 -- Auditoria adversarial final (Fase 13)

<!-- markdownlint-disable MD029 -->

**Auditor:** Fable / grok-4.6 (contexto limpo; nao implementou as fatias)
**Data:** 2026-08-16
**HEAD auditado:** `db3ee8a06c9e` (`feat(test): corpus Fase 10, dogfood cutover e margem do guard`)
**Working tree:** limpa de codigo; untracked so `PLANO-MELHORIA-...md` e `prompts_legados/`
**Metodo:** assumir que a equipe esta errada; tentar responder **SIM** (falha) a cada pergunta; so aceitar **NAO** com evidencia reproduzivel. Testes e poc em `/var/tmp` (arvore do repo intocada, salvo este relatorio).

> Papel de auditor: nada foi consertado. Relatorio de agente anterior nao e prova.

---

## 0. Estado medido (nao declarado)

| Superficie | SHA / fato |
|---|---|
| `HEAD` local `main` | `db3ee8a06c9e` |
| `origin/main` (GitHub) | `ed239a1` -- **1 commit atras** do checkout vivo |
| tag mais nova | `v1.1.0` = `319dd5e06a5cab39c88d0f4149fd18b3b8aaa284` |
| pin `claude-memory` `skills/tab_pendencias` | `319dd5e` (`v1.1.0`) -- **15 commits atras** do HEAD local; `tools/session_signals.py` **nao existe** nesse pin |
| `~/.claude/githooks` | symlink para `.../tab_pendencias/tools/hooks` (checkout de **dev**) |
| `~/.grok/skills/tab_pendencias` | symlink para o checkout de **dev** (`db3ee8a`) |
| `~/.claude/skills/tab_pendencias` | submodulo `319dd5e` / v1.1.0 |
| SessionStart Claude vivo | `python3 $HOME/.claude/hooks/tab_pendencias_reminder.py` (arquivo **outro**, 313 linhas) |
| Adapter do produto | `tools/hooks/tab_pendencias_reminder.py` (118 linhas, so wiring de `session_signals`) |
| md5 dos dois reminders | `cbfe626c...` vs `c5f215cf...` -- **divergem** |
| dogfood neste repo | `classifiable==0` OK; `n_items=111`; `n_verif=94` (`TAB_VERIFICATION_AGING`) |
| suíte amostral | `75 passed` em `test_wsjf` + `test_bus_contract` + `test_fase10_properties` + `test_hub_guard` + `test_session_signals` + `test_fase10_mutation` + `test_concurrent_inbox` + `test_todo_lock` |

`submodule_pin_drift.py` contra `~/.claude` + URL do GitHub: `status: atualizado` (pin == ultima **release** `v1.1.0`). Nao compara contra `main` nao taggeada.

---

## 1. As 15 perguntas

Convencao: **SIM** = o caminho de falha existe. **NAO** = nao foi possivel prova-lo no produto em `HEAD`. Residual documentado no `TODO.md` nao fecha a pergunta; so isenta o veredito global se nao houver SIM sem item.

### 1. Trabalho novo fica INBOX pra sempre sem sinal?

**NAO** (motor do produto). **Caveat de implantacao em Q11.**

- Linha sem `[triage ...]` valido e `classifiable=True` por definicao (`todo_lib.py` `inbox_entries` / `parse_triage_metadata`: `since`+`reason` obrigatorios).
- `session_signals.collect_signals` liga `TAB_TRIAGE_REQUIRED` se `classifiable>0` **ou** residual aged **ou** `inbox/*.md` (`session_signals.py:321-332`).
- Residual valido envelhece por `cycles>=2` **ou** idade `>=1` dia (`residual_is_aged`, `todo_lib.py:627-660`). Calendario avanca sem `--drain`.
- `needs-leader-decision` fresco e residencia ilimitada **por desenho** (ADR-0002 T3.4), mas **nao e invisivel**: ao envelhecer emite `TAB_LEADER_DECISION_AGED` + entra no TRIAGE.
- `--add` residual recusa gravar sem metadado parseavel (`todo_intake.py:2000-2003`).

O sinal obrigatorio do ADR (T3.2) e o hook de sessao. O arquivo que o Claude **realmente** dispara nao chama `collect_signals` (Q11). Isso nao e falha do motor; e binding vivo.

### 2. Classifiable sobrevive apos `--add`/`--drain`?

**NAO** no apply bem-sucedido. **Pior: no apply falho a linha some (Q14), nao sobrevive.**

- `--add --apply` aborta se ainda ha classifiable (`enforce_empty_classifiable`, `todo_intake.py:1762-1768`).
- `--drain --apply` exige judgment; pos-condicao `classifiable_inbox_count == 0` ou `rc=1` (`todo_intake.py:2342-2354`).
- `keep` reescreve a linha com `[triage since=... reason=...]` valido (deixa de ser classifiable).
- Testes: `tests/test_todo_intake.py` drain integrate/split; `tests/test_fase10_properties.py` `classifiable_zero_after_apply`; F10-22.

### 3. Item some entre inbox concorrente e TODO?

**NAO** no nucleo mecanico. Risco operacional por overclaim da skill.

- `concurrent_inbox.py` so tem `write_discovery` / `list_pending` / `read_discovery`. **Nao ha delete.**
- `--drain` **nao le** `inbox/*.md` (so a secao INBOX do `TODO.md`).
- Sinal `TAB_CONCURRENT_INBOX_PRESENT` + `TAB_TRIAGE_REQUIRED` persiste enquanto o arquivo existir (`session_signals.py:314-329`).
- Dedup de `--add` nao consulta `inbox/`; dois arquivos da mesma descoberta nao se apagam.

Overclaim: `SKILL.md:85-87` e `:182-186` dizem que `--drain` / `--create` / `--reorder` **esvaziam o `inbox/`**. O motor de `--drain` nao o faz. Um agente que "esvaziar" apagando `inbox/*.md` apos um `--drain` verde **perderia** trabalho. A regra de uniao (`SKILL.md:84`) contradiz essa prosa. Nao e perda no Python; e armadilha de briefing.

### 4. Duas sessoes geram duplicata silenciosa?

**NAO** nas rotas mecanicas.

- Apply serializa com `TodoWriteLock` (`todo_lock.py`; `run_intake`/`run_drain`).
- P-dup por ID na tabela **ou** descricao normalizada (TAB-ADD-002). Segundo apply sai `DUPLICATE`, sem linha nova.
- `write_discovery` em colisao de path acrescenta `-2`, `-3` (visivel, nao silencioso).
- Duplicata **semantica** (prosa diferente, mesmo trabalho) e fronteira agentiva declarada -- duas linhas visiveis, nao last-write-wins.

Nao foi possivel dois `--add --apply` no mesmo ID sem o segundo ser `DUPLICATE` ou lock timeout.

### 5. WSJF coloca dependente antes do prereq?

**NAO.**

- FULL: ciclo aborta (`dependency_cycle`); niveis via `_topo_levels`; `order_levels_then_wsjf` ordena nivel primeiro (`wsjf.py:438-466`, `todo_intake.py:1277-1302`).
- Mutation `topology_before_wsjf -> identity` morre (`tests/test_fase10_mutation.py`).
- Propriedade `topology_valid` + corpus F10. 75 testes amostrais verdes.

Grafo so e tao bom quanto o `Pré-requisito` declarado; `CHK-05`/`CHK-06` pegam id inexistente e ciclo. Isso nao e WSJF furando topologia.

### 6. Pedido do bus influencia ranking por retorica?

**NAO.**

- `extract_facts` / `candidate_from_bus`: `prioridade=""` sempre; `claimed_priority` so eco; `"urgente"` nao e fib (`bus_contract.py:250-305`, `wsjf.py:118-148`, `BUS_SOURCES` ignora label).
- `time_criticality` so int Fibonacci explicito.
- Corpus `tests/corpus/bus/consumer_a_urgent_rhetoric.json`; propriedade `sender_no_priority`; mutante `BUS_SOURCES` vazio morre.

### 7. Full reorder por simples passagem do tempo?

**NAO.**

- `session_signals` nao importa `run_intake` / `FULL_REORDER` / `wsjf` (teste de fronteira em `test_session_signals.py`).
- Idade/ciclos pedem `--drain`, que em residual **so incrementa `cycles`** -- nao integra, nao reordena (`todo_intake.py:2271-2282`).
- Integrar classifiable exige judgment explicito. Foundation -> FULL e julgamento, nao relogio.
- Contrato: `references/sinais-de-frescor.md` TAB-HOOK-003.

### 8. WIP (`🔄`) preemptado por feature pequena?

**NAO.**

- FULL pina ids com `_status_kind_andamento` como **barreira de segmento** (`todo_intake.py:1295-1301`, `wsjf.py:417-435`): livre nao atravessa pin.
- Status de existentes nao muda no reorder (W1).
- Testes: `TODO_WIP_PIN` em `test_todo_intake.py`; `test_prop_apply_2_full_topology_and_wip` (indice `#02` < `#04` mesmo com WSJF 20/20/20/1 no peer).

### 9. GitHub atrasado da instalacao viva sem recovery acusar?

**SIM.** Residual documentado: `TAB-REL-004` (⏳), `TAB-REL-002` (⏳).

Evidencia medida agora:

- Checkout vivo = `db3ee8a`; `origin/main` = `ed239a1` (falta o commit TAB-TST/TAB-CUT).
- `scripts/recovery_drill.py` copia o **product-root local** (`_PRODUCT_COPY`); nao faz `ls-remote`, nao compara SHA remoto, nao clona `claude-memory`.
- `tests/test_recovery_drill.py` / `test_fase10_e2e_install.py` exercitam o mock, nao o GitHub.

Um recovery verde **nao prova** que o GitHub recupera a instalacao viva. O detector de pin compara contra **tag** de release, nao contra `main` nao publicada.

### 10. `claude-memory` pina submodulo velho e passa todos os gates?

**SIM** (possibilidade + estado relativo ao HEAD). Residual: `TAB-SOT-007` (snippet CI **nao ligado**), `TAB-REL-003` (⏳).

- Pin vivo = `319dd5e` / v1.1.0. HEAD do produto = 15 commits depois; `session_signals.py` **nao esta** no pin (`git cat-file -e 319dd5e:tools/session_signals.py` falha).
- Este repo **nao** tem job que rode `submodule_pin_drift` contra o consumidor. CI daqui verde nao ve o gitlink de `claude-memory`.
- `TAB-SOT-007` declara o gemeo CI "documentado e NAO ligado: ligar pertence ao claude-memory".
- Contra a ultima **release**, o pin atual e `atualizado` (medido). Contra o HEAD da campanha, e velho e silencioso.

### 11. Hook global e produto com regras diferentes de frescor?

**SIM.** Residual parcial: `TAB-VAULT-004` (snippet entregue, **nao aplicado** no settings vivo); `HOOKSRC-1` (⏳) cobre githooks, nao o SessionStart.

Medido:

| | Claude vivo | Produto `HEAD` |
|---|---|---|
| Arquivo | `~/.claude/hooks/tab_pendencias_reminder.py` (313 linhas) | `tools/hooks/tab_pendencias_reminder.py` (118 linhas) |
| Config | `.tab-staleness.json` (`commits`+`dias` modo **e**, `horas_sessao`) | `.tab_pendencias.ini` `[signals]` |
| INBOX / classifiable / aged / `inbox/` | **nao olha** (so menciona INBOX em prosa de reorder) | `TAB_TRIAGE_REQUIRED` etc. |
| Gate | so age com `.bigtech-porte` | qualquer repo com/sem TODO |
| Eventos | SessionStart + UserPromptSubmit (nudge de horas) | wiring fail-open de `collect_signals` |

`~/.claude/settings.json:243` e `:259` apontam para o arquivo antigo. O snippet do produto (`templates/vault/settings.sanitized.hook-snippet.json`) pede `skills/tab_pendencias/tools/hooks/tab_pendencias_reminder.py`. O submodulo vivo ainda e v1.1.0 **sem** `session_signals.py` -- mesmo reapontando o settings, o pin antigo nao tem o motor novo.

Githooks (`core.hooksPath` -> symlink do **dev**) e SessionStart (copia antiga) **ja divergem entre si nesta maquina**.

### 12. Hub vira fonte concorrente?

**NAO** na rota de intake/drain apply.

- `[hub] derived=true` recusa `--apply` (`is_derived_hub`, testes `test_hub_guard.py`, F10-26).
- Sem a flag, o arquivo nao e hub (projeto normal).
- Guard **nao** cobre `todo_sync` / `todo_fix` / edicao manual. `TAB-HUB-GEN` (⏳) ainda nao regenera a view. Abaixo do limiar de SIM: sem flag nao ha hub; com flag o intake nao grava.

### 13. Teste verde porque fixture viva mudou?

**NAO.**

- `tests/test_contract_real_corpus.py` le snapshot imutavel no `.pytest_cache` (`_frozen_fixture_text`); `_read_live_fixture` so na captura/regen explicita (`TAB_PENDENCIAS_REGEN_BASELINE_CONTR1`).
- Corpus F10 e `tests/corpus/bus/` sao sinteticos versionados.
- Guard anti-fixture-real ativo. Regeneracao e skip de setup, nao pass silencioso.

### 14. Migracao apaga linha antiga da INBOX?

**SIM.** **Nao ha item no TODO.md.** Reproduzido.

`run_drain` apply, em `_run_drain_inner`:

1. valida judgments e monta candidatos (`todo_intake.py:2233-2269`);
2. **apaga** as linhas `integrate`/`split` no texto (`:2303-2311`);
3. **grava** o TODO intermediario (`:2313-2318`);
4. so entao chama `run_intake` (`:2320-2334`).

Se o passo 4 falha, `DrainResult.applied=False` e `rc=1`, mas o arquivo **ja perdeu** a linha. Journal **nao** e escrito (intake aborta antes do write-ahead quando falta `item_id`). Sem orfao -> **sem** `TAB_INTAKE_RECOVERY_REQUIRED`. Trabalho irrecuperavel pelo motor.

Poc (2026-08-16, `/var/tmp/aud-extreme-01-drain-loss`, hooks isolados):

```text
INBOX antes: ['LEGACY-1: old inbox line without triage metadata']
judgment integrate com candidate_id, sem item_id, is_local=True
RC 1  applied False
error: LOCAL_INTEGRATION exige item_id nao vazio
INBOX depois: []
tabela: so #01
LEGACY-1 still in file? False
journal dir: ausente
```

`_candidate_from_judgment_dict` aceita `candidate_id` sem `item_id` (`:2036-2046`). A validacao pre-escrita nao exige o id da linha nova. Cutover de INBOX **legada** (TAB-CUT-001) usa exatamente essa forma de linha.

Isto viola ADR-0002 T2 (nada se perde apos captura) e TAB-CUT-005 ("nunca apagar candidatos").

O mesmo padrao quebra o **segundo** item de um `split` se o primeiro intake passar e o segundo falhar: a linha original ja foi stripada.

### 15. Agent declara "integrado" sem o `main` repetir a prova?

**NAO** como API do produto.

- Nao existe flag/self-attestation que marque item integrado.
- Persistencia so via `run_intake` + invariantes + journal `DONE` **depois** da prova (`todo_intake.py:1871`, `:1949`).
- Contrato `templates/agents/implementer-discovery-contract.md`: agent devolve `DISCOVERED_WORK`; main chama o motor.
- Relatorio de agent mentir em prosa continua possivel (regra da casa 10 / plano). Nao e estado do produto.

---

## 2. Resumo

| # | Pergunta (curta) | Veredito | Residual TODO? |
|---|---|---|---|
| 1 | INBOX eterna sem sinal | NAO (motor) | -- (caveat Q11) |
| 2 | classifiable apos add/drain | NAO | -- |
| 3 | perda inbox/ <-> TODO | NAO (motor) | overclaim SKILL |
| 4 | duplicata silenciosa | NAO | -- |
| 5 | WSJF fura prereq | NAO | -- |
| 6 | retorica do bus | NAO | -- |
| 7 | FULL por relogio | NAO | -- |
| 8 | preempt WIP | NAO | -- |
| 9 | GitHub atrasado, recovery calado | **SIM** | `TAB-REL-002`, `TAB-REL-004` |
| 10 | pin velho passa gates | **SIM** | `TAB-SOT-007`, `TAB-REL-003` |
| 11 | hook global != produto | **SIM** | `TAB-VAULT-004` (parcial), `HOOKSRC-1` |
| 12 | hub fonte concorrente | NAO | `TAB-HUB-GEN` (gerador, outro tema) |
| 13 | verde por fixture viva | NAO | -- |
| 14 | migracao apaga INBOX | **SIM** | **nenhum** |
| 15 | "integrado" sem prova do main | NAO | -- |

Testes amostrais (75) verdes **nao** exercitam a janela strip-then-intake do drain.

---

## 3. Achados (so os que importam)

### Critico -- perda de trabalho no `--drain` apply

- **Confianca:** 100 (poc no disco).
- **Onde:** `tools/todo_intake.py:2303-2334`.
- **Por que:** escrita intermediaria **antes** do destino terminal; falha do intake deixa zero rastros (sem journal, `applied=False` mente).
- **Conserto minimo (nao aplicado nesta auditoria):**
  1. Nao gravar strip ate **todos** os `run_intake` passarem (montar texto final em memoria, um `os.replace`).
  2. Ou gravar journal `NEW` da linha original **antes** de qualquer strip, e restaurar a linha se intake falhar.
  3. Validar `item_id` nao vazio nas rotas L0/SCOPED/FULL **antes** de mutar a INBOX.
  4. Teste de regressao: o poc acima deve ficar vermelho ate o conserto; `applied=False` implica `TODO.md` byte-identico ao pre-apply.

### Importante -- SessionStart vivo nao e o motor de `TAB_*`

- **Confianca:** 100 (settings + md5 + leitura do fonte).
- **Onde:** `~/.claude/settings.json:243,259` vs `templates/vault/settings.sanitized.hook-snippet.json` vs pin `319dd5e` sem `session_signals.py`.
- **Efeito:** na sessao Claude real, INBOX classifiable / residual aged / `inbox/` **nao** geram sinal obrigatorio. O consumidor da campanha continua voluntario. Grok (symlink do dev) e o githook de dev veem o motor novo; o SessionStart nao.
- **Nao fecha so com o snippet:** precisa pin >= revisao que contem `session_signals.py` **e** rebind do settings.

### Importante -- recovery nao compara GitHub x vivo

- **Confianca:** 95 (codigo do drill + `ls-remote` vs `HEAD`).
- Documentado em `TAB-REL-004`; ainda ⏳. Confirmado: 1 commit local nao esta no GitHub e o drill nao acusaria.

---

## 4. DISCOVERED_WORK (para o main, nao editado no TODO)

```text
DISCOVERED_WORK
source_item: TAB-INBOX-004
description: run_drain apply apaga linha classifiable da INBOX antes do run_intake; se o intake falha a linha some sem journal e DrainResult.applied=False mente
evidence: tools/todo_intake.py:2303-2334; poc /var/tmp/aud-extreme-01-drain-loss (LEGACY-1 sumiu, rc=1, sem journal)
known_dependencies: TAB-INBOX-003, TAB-CUT-001, TAB-CUT-005
blast_radius: component
item_id: TAB-INBOX-005
```

```text
DISCOVERED_WORK
source_item: TAB-VAULT-004
description: SessionStart/UserPromptSubmit vivos ainda apontam para ~/.claude/hooks/tab_pendencias_reminder.py (regras .tab-staleness.json); nao emitem TAB_*; pin v1.1.0 nao tem session_signals.py
evidence: ~/.claude/settings.json:243; md5 distinto; git cat-file -e 319dd5e:tools/session_signals.py falha
known_dependencies: TAB-HOOK-004, TAB-REL-003, HOOKSRC-1
blast_radius: system
item_id: TAB-VAULT-006
```

```text
DISCOVERED_WORK
source_item: TAB-CONC-003
description: SKILL.md afirma que --drain/--create/--reorder esvaziam inbox/; o motor de --drain nao consome inbox/*.md (nao ha delete). Agente que apagar arquivos apos drain perde descoberta
evidence: SKILL.md:85-87 vs tools/todo_intake.py run_drain (so inbox_entries) vs concurrent_inbox.py (sem unlink)
known_dependencies: TAB-CONC-002, TAB-INBOX-004
blast_radius: local
item_id: TAB-CONC-005
```

---

## 5. O que a campanha ja segura (para nao reabrir)

Motor de intake + exception queue + lock + WSJF topologico + pin de WIP + contrato do bus + snapshot de fixture + aging hibrido + hub derived no apply: **nao encontrei caminho de falha** nas perguntas 2, 4-8, 12, 13, 15. Mutation F10 (topologia, L0 forcado, aged sempre falso, BUS_SOURCES vazio) continua vermelha no mutante.

Isso **nao** autoriza fechar a campanha enquanto Q14 existir sem item e sem conserto.

---

## 6. Veredito

**CAMPANHA ABERTA.**

Qualquer SIM mantem aberta. Q9/Q10 tem residual no TODO. Q11 tem residual parcial. **Q14 e SIM sem item** e e perda de trabalho na rota de cutover/migracao -- o criterio de falsificacao da campanha.

Nao fechar, nao taggear, nao declarar liveness da INBOX ate:

1. o strip-before-intake do `--drain` ter teste vermelho + conserto + prova de que `applied=False` implica arquivo intocado;
2. o binding SessionStart + pin `>=` revisao com `session_signals` estarem no caminho vivo (ou o lider rejeitar explicitamente e registrar).

---

## Adendo pos-correcao (mesma sessao, apos AUD)

**Q14 (perda no drain):** corrigido em `tools/todo_intake.py` -- intake dos candidatos
ocorre **antes** do strip da origem; `skip_classifiable_ids` evita o gate bloquear a
propria linha; testes `test_drain_intake_fail_preserva_inbox_*` e mutacao. Item
`TAB-INBOX-005` em 🔍.

**Q9/Q10/Q11** permanecem residuais de **publicacao/distribuicao** (pin de submodule,
rebind SessionStart no vault), nao do motor. Itens TAB-REL-* e documentacao de templates.

Veredito de *motor* apos o fix Q14: as perguntas 1-8, 12-15 do motor respondem NAO
com o codigo desta revisao. Campanha de *produto motor* fechavel; campanha de
*distribuicao vault* segue com TAB-REL-003/004 e rebind de hook.

