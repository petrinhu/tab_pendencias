# TODO -- tab_pendencias v2 (skill + toolkit distribuíveis)

> Tabela canônica de pendências. **A ordem das linhas É a ordem de execução recomendada**;
> a coluna `Onda` marca os passos de igual valor que podem rodar em paralelo.
> Fonte dos fatos: `prompt_inicial.md`. Decisões fechadas: `decisoes_lider.md`.
> Manuais: `TESTES.md`, `AUDITORIAS.md`. Porte: **early / Pipeline-Lean**, kanban, **WIP = 1 onda**.
>
> **Dogfooding**: este arquivo usa a própria skill que o projeto constrói (schema de 9 colunas,
> INBOX, ondas de teste/auditoria downstream, item de Wiki como última onda pós-tag).

| ID | Onda | Grupo | Descrição Técnica | Prioridade | Pré-requisito | Dificuldade | Status | Estado Auditado |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| W0-DEC | — | Governança | Kickoff `/bigtech`: porte early classificado por Cósimo, mapa de ativação aprovado, e as 8 decisões **D-1..D-8** resolvidas pelo líder (registro em `decisoes_lider.md`). Gate anti-OE do `--create` decidido por Cósimo: **thread direta** + 1 passe de ADR. | Alta | — | Baixa | ✅ Concluído | — |
| ADR-1 | W1 | Arquitetura | ADR da fronteira **núcleo genérico × convenções da casa** (nenhum check do núcleo pode depender do perfil "casa"), do contrato do parser (round-trip byte-exato, compat 8/9 colunas) e dos limites de `--audit`/`--fix`. Executor: `software-architect`. É o risco arquitetural real do projeto e precisa existir ANTES dos consertos, não como auditoria tardia. Entregue em `docs/adr/0001-*.md`; gerou as decisões D-9/D-10/D-11. | Alta | — | Média | 🔍 Pendente verificação | — |
| SCAF-1 | W1 | Scaffold | Clonar o repo em `Projects/tab_pendencias/`, montar o layout monorepo da D-4 (`tools/`, `tools/hooks/`, `tests/`, `references/`) preservando os 21 commits, **e relicenciar o repo inteiro para GPL-3.0-or-later** (D-3): trocar `LICENSE`, cabeçalhos dos fontes, purgar menção residual à PolyForm no README/SKILL.md. Publicação de licença é irreversível na prática. | Alta | — | Média | 🔍 Pendente verificação | — |
| ABS-1 | W2 | Scaffold | Absorver de `~/.claude/githooks/`: os 4 scripts (`todo_lib`, `todo_sync`, `todo_health`, `todo_freshness`) para `tools/`, e `_chain.sh` + os 7 shims POSIX para `tools/hooks/`; mover a suíte pytest (**41 testes**) para `tests/` e deixá-la 100% verde no novo layout. Atômico: absorver os `.py` sem os testes deixa o repo vermelho. | Alta | SCAF-1 | Média | 🔍 Pendente verificação | — |
| DOC-FRESH | W2 | Docs | Internalizar em `references/` a parte NORMATIVA das regras de frescor (D-2), hoje só em `~/.claude/docs/tabela-pendencias-frescor.md`; o vault fica com o histórico/racional. Sem isto, quem clona não recebe a norma que a skill exige. | Média | SCAF-1 | Baixa | 🔍 Pendente verificação | — |
| CONTR-1 | W3 | Testes | Testes de **contrato** com as fixtures reais (consumidor A **116** itens, consumidor B **215**), mantidas apenas LOCALMENTE e nunca commitadas sem anonimização. Vem ANTES dos consertos de propósito: é a rede de segurança que prova que nenhuma mudança quebra os consumidores vivos. | Alta | ABS-1 | Baixa | 🔍 Pendente verificação | — |
| CORP-0 | W3 | Testes | Corpus **sintético** de aceitação (AC-0): outra língua, outro esquema de ID (`#NN`, `XYZ.9.9`), outra estrutura de seções; property-based sobre IDs gerados. Cap anti-OE do Cósimo: **no máximo 2 alfabetos e 2 formatos de ID**. É o que prova que a skill é agnóstica a projeto. | Alta | ABS-1 | Média | 🔍 Pendente verificação | — |
| CI-1 | W3 | CI | Workflow único do GitHub Actions com jobs: pytest em **matrix ubuntu + windows** (a promessa de Git-for-Windows não se sustenta só com ubuntu), `shellcheck` + smoke dos shims, lint de Markdown, `gitleaks` (secrets), guard "**nenhum import fora da stdlib**" e guard **anti-vazamento de fixture real** (repo público). Actions pinadas por SHA. | Alta | ABS-1 | Média | 🔍 Pendente verificação | — |
| CI-2 | W4 | CI | Ampliar a matrix de pytest do `CI-1` para **Debian, Fedora e Arch** via `container:` sobre `ubuntu-latest` (sem GitHub runner nativo dessas distros), somando-se a ubuntu+windows nativos (não os substitui). Imagens pinadas por tag+digest (nunca `latest` solto). Smoke dos shims também sob o `/bin/sh` do container Debian. Validado localmente com `podman` (as 3 imagens, pytest completo + smoke). Achados registrados no próprio workflow: (1) Ubuntu **já** usa dash como `/bin/sh` (a premissa original de "1ª exposição a shell estrito" estava errada, corrigida in-loco); (2) piso/teto de Python medido = Debian 3.11.2, Fedora 3.13.9, Arch 3.14.6; (3) jobs de container rodam como **root** por padrão, o que produz falhas espúrias em testes sensíveis a permissão (chmod) — não corrigido nesta fatia (privilege-drop portátil entre apt/dnf/pacman é decisão maior), apenas documentado no job. | Média | CI-1 | Média | 🔍 Pendente verificação | — |
| SANIT-1 | W4 | Segurança | **Incidente de privacidade (28/07)**: os arquivos internos de planejamento foram versionados num repo PÚBLICO por erro de orquestração, expondo caminhos da máquina do autor, nomes de projetos privados e o bus interno. Conserto: tirar os 3 arquivos do versionamento (mantidos em disco, no `.gitignore`), designação neutra "consumidor A/B" nos docs, caminhos de fixture vindos de variável de ambiente, e redesenho do guard anti-vazamento (que continha os próprios termos que deveria proteger). Histórico reescrito com `filter-repo` + force-push, autorizado pelo líder; verificado por clone fresco do remoto. | Alta | CI-1 | Média | 🔍 Pendente verificação | — |
| BUG-5 | W4 | Consertos | Classificação de status por **emoji-prefixo** (D-1), com fallback word-boundary só para tabela legada sem emoji + aviso no `--audit`. Hoje `is_awaiting_verification` casa "VERIFICADO" e `is_done` casa "inconclusivo" (`todo_lib.py:169-174`); cobre também SUB-1 (`"🔴 Bloqueado (dependente de X)"` → pendente) e SUB-2 (`"⏳ Pendente (verificar disponibilidade)"` → bloqueia flip legítimo). Afeta todos os repos do líder. | Alta | ABS-1, ADR-1, CONTR-1 | Média | 🔍 Pendente verificação | — |
| CLI-1 | W4 | Consertos | Contrato de CLI da D-6 nos 3 scripts: `argparse` + `--help`; exit `0`=ok, `1`=erro de execução, `2`=achados; **flag desconhecida = erro** (hoje `--aply` é ignorado em silêncio e `todo_sync.main` devolve 0 até quando nem é repo git). | Média | ABS-1 | Média | 🔍 Pendente verificação | — |
| GAP-1 | W4 | Testes | Suíte do `todo_health.py` do zero -- é o único módulo hoje sem nenhum teste. | Média | ABS-1 | Baixa | 🔍 Pendente verificação | — |
| GAP-2 | W4 | Testes | Teste e2e de `todo_freshness.main()` (o caminho real, com `diff-tree`), hoje sem cobertura nenhuma. | Média | ABS-1 | Média | 🔍 Pendente verificação | — |
| HOOKISO-1 | W5 | Testes | Isolar os testes que fazem `git init` do `core.hooksPath` **GLOBAL** desta máquina (`~/.gitconfig` → `~/.claude/githooks/`): sem proteção, todo commit num repositório temporário de teste dispara o hook AMBIENTE de verdade por baixo dos panos, poluindo `$GIT_DIR/todo-freshness.log` e quebrando a determinação/isolamento da suíte. Helper `git_init_isolado` (config `core.hooksPath` **local**, nunca `--global`) centralizado em `tests/conftest.py`; aplicado em `test_cli_contract.py` (que tinha o gêmeo do achado, sem proteção nenhuma) com 2 testes de prova. | Média | CLI-1 | Baixa | 🔍 Pendente verificação | — |
| SPRAWL-1 | W5 | Consertos | Fim explícito da tabela canônica, revisado por **D-12**: a tabela ATRAVESSA um heading quando o que vem depois é continuação (mesmo schema); ENCERRA ao encontrar 2º cabeçalho ID+Status, tabela de schema diferente (mesmo com ncols coincidente), ou fim do documento. `CONTR-1` verde: 116=116, 215=215 medidos pós-conserto. | Alta | ABS-1, ADR-1, CONTR-1 | Média | 🔍 Pendente verificação | — |
| BUG-1' | W5 | Consertos | ID duplicado passa em silêncio: `parse_status_map` deixa a última linha vencer (`todo_lib.py:144-147`) e `by_id` idem (`todo_sync.py:122`); nenhum teste detecta. A instância foi removida do dado, a CLASSE segue aberta. | Alta | ABS-1 | Baixa | 🔍 Pendente verificação | — |
| PRED-FIX | W5 | Consertos | Três predicados de uma linha, um commit citando os 3 IDs: **CIT-1** `cited_ids("fechei V-12.")` → `[]` (ponto final cai no lookahead, `todo_lib.py:177-185`); **TCH-1** `touched_code(["src/inbox/parser.py"])` → False (substring `/inbox/` em path de código, `todo_lib.py:49-62`) -- restringir ao `inbox/` do topo; **HDR-1** `_is_header` dispara com qualquer célula contendo "status" (`todo_lib.py:100-102`). | Alta | ABS-1 | Baixa | 🔍 Pendente verificação | — |
| ENC-1 | W5 | Consertos | Unificar a política de exceção/encoding: `todo_freshness.main` engolia QUALQUER exceção de leitura (`todo_freshness.py:81`) enquanto `todo_sync`/`todo_health` crashavam cru em `UnicodeDecodeError`; agora `todo_freshness` continua exit 0 (warn-only) mas não mais em silêncio (stderr + log `erro=leitura_todo:<Tipo>`), e `todo_sync`/`todo_health` viram mensagem clara + exit 1, com flag `-v/--verbose` (traceback completo) nos 3 scripts. BOM/CRLF exercitados ponta-a-ponta. Não é polimento: **CRLF/BOM é o Windows do claim cross-platform**. | Alta | ABS-1 | Baixa | 🔍 Pendente verificação | — |
| ROOT-1 | W5 | Consertos | `todo_freshness.py` chamava `git diff-tree --no-commit-id --name-only -r HEAD` **sem `--root`**: no commit RAIZ de um repositório (sem pai) esse comando devolve saída VAZIA, então `touched_code(files)` era sempre `False` ali e o aviso "tocou código mas não citou ID" nunca disparava no primeiro commit de um repo. Consertado com `--root`. `todo_sync.py` usa `git log --name-only` (não `diff-tree`) para o mesmo propósito, e `git log` já mostra os arquivos do commit raiz sem precisar de `--root` -- confirmado ao vivo; **não tinha o mesmo bug**. | Alta | ABS-1 | Baixa | 🔍 Pendente verificação | — |
| AUDIT-ENG | W6 | Audit | `tools/todo_audit.py`: motor dos checks + CLI própria (D-6). Relatório numerado por check com severidade CRÍTICO/IMPORTANTE/COSMÉTICO, evidência `linha:`, marcação **[auto-fixável]** vs **[julgamento]**, saída legível no chat + arquivo opcional em scratchpad (nunca no repo), e a regra **"no silent caps"**: todo descarte ou limitação do próprio audit é declarado no relatório. | Alta | CLI-1, SPRAWL-1, BUG-5 | Alta | 🔍 Pendente verificação | — |
| BUS-1 | W6 | Relay | Relay ao consumidor B pelo bus interno de comunicação entre sessões (thread `higiene-repo`): comunicar a decisão **D-1** (emoji-prefixo) e o cronograma do conserto. Ele está **bloqueado esperando isto** para os dois repos não divergirem. Barato e desbloqueia terceiro -- por isso sobe na fila. | Alta | BUG-5 | Baixa | ⏳ Pendente | — |
| CHK-CORE | W7 | Audit | Integridade de tabela e vocabulário: **CHK-01** ID duplicado (listar as N linhas com diff resumido); **CHK-02** nº de células ≠ ncols, com DIAGNÓSTICO da causa provável (pipe cru? célula faltando? fragmento truncado?) e conserto sugerido; **CHK-03** múltiplos cabeçalhos ID+Status + relatório do SPAN da canônica; **CHK-04** ncols divergente entre tabelas (pré-condição de consolidação); **CHK-08** status fora do vocabulário; **CHK-11** reconciliar o total do `todo_health` com contagem independente (ele já reportou 14 num arquivo de 215 sem avisar). | Alta | AUDIT-ENG | Alta | 🔍 Pendente verificação | — |
| CHK-GRAPH | W7 | Audit | Um walk no grafo de dependências: **CHK-05** pré-requisito citando ID inexistente; **CHK-06** ciclo (reportar o ciclo inteiro); **CHK-07** Onda inconsistente (item na mesma onda de um pré-requisito seu, ou posicionado antes dele na ordem das linhas). | Alta | AUDIT-ENG | Média | 🔍 Pendente verificação | — |
| CHK-09 | W7 | Audit | Claims obsoletas na Descrição ("não pushado", "commit local", "branch X") **verificadas contra o git real** (`git ls-remote`, `cat-file -e`, `diff main --`). Padrões configuráveis com defaults pt+en -- nenhum check pode assumir português no conteúdo livre. | Média | AUDIT-ENG | Média | 🔍 Pendente verificação | — |
| CHK-10 | W7 | Audit | Rodar `todo_sync.py` **sem** `--apply` e anexar a proposta ao relatório, com o aviso canônico (citar ID ≠ entregar; ler com desconfiança) e o estado do baseline `todo-sync-ref`. | Média | AUDIT-ENG | Baixa | 🔍 Pendente verificação | — |
| AUDIT-PATH | W7 | Audit | `--todo <caminho>`: audita um `TODO.md` fora do cwd/repositório corrente (achado do team-lead ao tentar medir as fixtures de aceitação -- sem isto `AC-REAL` é impossível, pois as fixtures nunca podem ser copiadas para dentro do repo público). Restrição deliberada: o arquivo precisa se chamar exatamente `TODO.md` (mesma convenção de `todo_lib.find_todo`), para `CHK-11` (que re-descobre `root/TODO.md` por caminho de código independente) não comparar contra o arquivo errado. Quando o alvo está fora de qualquer repositório git resolvível, o motor declara isso uma única vez (no silent caps) em vez de deixar `CHK-09`/`CHK-10` degradarem achado a achado sem explicação sistêmica. Read-only preservado e provado por hash nas duas fixtures reais. | Alta | AUDIT-ENG | Média | 🔍 Pendente verificação | — |
| GUARD-FP | W7 | CI | Falso positivo do guard "nenhum import fora da stdlib" (`CI-1`): `tools/todo_audit.py` importa `checks.chk_graph`/`chk_frescor`/`chk_core` -- pacote **deste próprio repositório** (`tools/checks/`), não dependência externa -- e o guard antigo só reconhecia stem de arquivo solto como módulo local, nunca exercitado contra um subpacote real. Conserto: resolução estrutural componente-a-componente (pacote com `__init__.py` ou módulo `.py`, igual à busca do importador do Python), não lista/stem fixo -- essa é a mesma classe de deriva de lista fixa já vista noutros guards. Provado com import de terceiro real plantado em cópia extraída (acusa) e removido (silencia). | Alta | CI-1 | Baixa | 🔍 Pendente verificação | — |
| BASELINE-FRAGIL | W7 | Testes | Teste de contrato `test_mapa_status_congelado_consumidor_b` comparava o **texto bruto** da célula de status da fixture real -- qualquer edição de prosa do dono do arquivo (fixture viva, editada por outra sessão) quebra o teste mesmo sem nenhuma reclassificação de verdade. 1ª rodada: baseline passou a congelar a **classificação derivada** (kind do emoji + predicados `is_pending`/`is_flip_eligible`/`is_awaiting_verification`/`is_done`), não o texto da célula. **2ª rodada (achado do team-lead):** isso não bastava -- a premissa de comparar contra o **arquivo vivo** a cada execução é frágil por desenho, porque outro projeto pode ser editado por outra sessão a qualquer momento, inclusive durante a suite (achado ao vivo: 2 itens do consumidor B perderam o emoji-prefixo reconhecido entre uma execução e a seguinte). Conserto: todos os testes de contrato passam a ler um **snapshot imutável** do texto da fixture (cache do pytest, nunca versionado, com metadados de origem/timestamp), congelado uma única vez e reusado até regeneração explícita -- nunca mais o arquivo vivo diretamente. Uma única variável de ambiente (`TAB_PENDENCIAS_REGEN_BASELINE_CONTR1`) regenera snapshot **e** baseline de classificação atomicamente (nunca só um dos dois, o que compararia classificação nova contra baseline velho). Provado por mutação com corpus sintético: edição do arquivo vivo (prosa ou reclassificação) não afeta mais nada após o snapshot congelado; mutação do **próprio parser** (predicado `is_done`) sobre o mesmo insumo congelado ainda acusa, nomeando o item. | Alta | CONTR-1 | Baixa | 🔍 Pendente verificação | — |
| LEAK-2 | W7 | Segurança | **Continuação do incidente `SANIT-1`**: 5 ocorrências de nome de projeto-fonte real, desta vez dentro do CONTEÚDO (comentário/docstring de teste), não do caminho -- vetor que a Camada 2 do guard não cobre (ela só checa caminho). Achadas e substituídas por designação neutra "consumidor A"/"consumidor B", sem perder o sentido técnico do comentário. Guard `guard_no_real_fixtures.py` ganhou **Camada 3** (conteúdo, opt-in, mesma fonte de termos das Camadas 2/3), com mascaramento do termo na própria saída (evita auto-vazamento) e aviso explícito quando as camadas opt-in estão desligadas (o "OK" antigo não distinguia proteção completa de proteção parcial). Provado por TDD com plantio/remoção de termo sintético em repositório descartável (exit 1 → exit 1→0). | Alta | SANIT-1 | Baixa | 🔍 Pendente verificação | — |
| SKILL-AUDIT | W8 | Audit | Integrar `--audit` na `SKILL.md` (contrato, formato de saída, exit codes) e atualizar o `argument-hint` do frontmatter (D-7). | Alta | CHK-CORE, CHK-GRAPH, CHK-09, CHK-10 | Média | 🔍 Pendente verificação | — |
| AC-REAL | W8 | Testes | Aceitação em corpus real, localizando **pelo conteúdo, não pelo número da linha**: **AC-1** consumidor B -- achar o `TODO-PARSER-BUG` invisível por pipe cru no próprio texto, o fragmento truncado de `ATOM-3`, e o sprawl da canônica; **AC-2** consumidor A -- reconciliar 116=116 com zero CRÍTICO (ou achados listados e explicados, nunca "passou" por omissão). | Alta | CHK-CORE, CHK-GRAPH, AUDIT-PATH | Média | 🔍 Pendente verificação | — |
| CHK-CASA | W8 | Audit | Camada de **convenção da casa** (opt-in, desligada por default): **CHK-12** TST-*/AUD-* agendados antes do que cobrem; **CHK-13** INBOX (IDs duplicando a tabela, formato); **CHK-14** item fixo de Wiki+doc-iniciante como última onda. Fora do perfil "casa" nem aparecem no relatório -- é o teste vivo da separação núcleo × casa. **Pode escorregar para v1.1 se W8 apertar.** | Baixa | CHK-CORE | Média | 🔍 Pendente verificação | — |
| FIX-ENG | W9 | Fix | `tools/todo_fix.py`: aplica APENAS o mecânico e byte-preserving -- escapar `\|` cru, remover fragmento truncado após mostrar o diff, consolidar tabelas fragmentadas (só com ncols idênticos, preservando ID/Status/Estado Auditado, **sem reordenar e sem recalcular WSJF**), corrigir claim obsoleta mostrando o texto novo antes. **NUNCA** muda status, reordena ou deleta branch/commit. Pré-condição: working tree do TODO.md limpa (aborta senão); depois de aplicar, re-parsear provando round-trip + contagem. Confirmação separada por classe de fix. Inclui a regra fixa do líder: ao final de todo `--audit`, sugerir o `--fix` listando o que faria. | Alta | AUDIT-ENG, CHK-CORE | Alta | 🔍 Pendente verificação | — |
| AC-FIX | W10 | Testes | Aceitação **AC-4**: `--fix` sobre uma **cópia** do corpus do consumidor B conserta os 2 defeitos vivos, mantém round-trip byte-exato no resto, e o `--audit` seguinte sai limpo desses checks. Gate obrigatório: o `--fix` reescreve arquivo do usuário e o erro aqui é irreversível do lado de fora. | Alta | FIX-ENG | Média | 🔍 Pendente verificação | — |
| SKILL-PATH | W10 | Docs | Conserto do caminho absoluto `~/.claude/githooks/` no `SKILL.md` (3 ocorrências: comando `todo_sync.py`, comando `todo_health.py`, e o README do toolkit), substituído por caminho relativo ao próprio repo (`tools/todo_sync.py`, `tools/todo_health.py`, `tools/README.md`) e pela referência normativa `references/frescor-da-tabela.md`. Achado por `DOC-FRESH` em 28/07, confirmado na fonte e corrigido nesta fatia. | Alta | ABS-1 | Baixa | 🔍 Pendente verificação | — |
| README-1 | W10 | Docs | README bilíngue corrigido para **9 colunas** (documentava 8 em `README.md:34-37` e no espelho en `:131-134`), com matriz de instalação, as duas camadas (núcleo genérico × convenções da casa), as dependências externas hoje não declaradas, e os modos degradados (sem agents → thread direta; sem vault → wikilinks mortos; sem python3 → só a parte agent-driven; Windows sem Git for Windows → perde só o sync mecânico via hook). **DoD inclui o check de consistência README × SKILL.md** -- o gate que teria pegado o schema obsoleto; conferido manualmente (schema de 9 colunas idêntico nos dois arquivos). Seções `--audit` (catálogo de 14 checks, core+casa) e `--fix` (escopo real de 2 classes, não as 4 do ADR; riscos conhecidos declarados) escritas nos dois espelhos, verificadas executando `tools/todo_audit.py` e `tools/todo_fix.py` de verdade. | Alta | SKILL-AUDIT, FIX-ENG | Média | 🔍 Pendente verificação | — |
| TST-T15 | W10 | Testes | Pre-CI local: `scripts/preci.sh` espelhando `.github/workflows/ci.yml` na mesma ordem (pytest, shellcheck + smoke dos shims, markdownlint-cli2, gitleaks, os dois guards; ruff como camada extra não-bloqueante), para o vermelho não aparecer só no servidor. Diferenças vs. o CI (matriz de 5 ambientes reduzida a 1, entre outras) declaradas na própria saída do script. Ver `TESTES.md`. | Média | CI-1, FIX-ENG | Baixa | 🔍 Pendente verificação | — |
| AUD-FINAL | W11 | Auditoria | Auditoria adversarial **única** por `qa-engineer` independente (≠ implementer, ≠ orquestrador), consolidando o que em projeto maior seriam 7 auditorias: mutation testing de cada guard, cobertura significativa, meia-página de superfície de ameaça do hook (código de terceiro executando na máquina alheia), SAST + secrets, e **conformidade de licença pós-GPL-3**. Arquitetura não vira item aqui porque virou ADR-1. Entregue em `docs/auditoria/AUD-FINAL-2026-07-28.md`: 2 achados CRÍTICO (bypass de `--output` via symlink em `todo_audit.py`; nomes reais de projeto ainda no histórico git pós-`LEAK-2`, fix forward-only não reescreveu histórico) bloqueiam a tag `v1.0.0` até correção; 2 IMPORTANTE (mutante sobrevivente na prova de round-trip do `--fix`; CAMADAS 1/2 do guard anti-vazamento sem teste automatizado) e 5 COSMÉTICO viram follow-up. Ver `AUDITORIAS.md`. | Alta | AC-REAL, AC-FIX, TST-T15 | Alta | 🔍 Pendente verificação | — |
| SYMLINK-1 | W12 | Segurança | Conserto do achado CRÍTICO de `AUD-FINAL`: `_output_forbidden` (`tools/todo_audit.py`) comparava caminhos por `os.path.abspath` (só normaliza a string), que não resolve link simbólico -- um symlink criado fora do repositório mas apontando para dentro dele contornava a proibição de `--output` e o `--audit` (contrato read-only) escrevia de fato dentro do repositório auditado. Trocado por `os.path.realpath` em `_output_forbidden` e no gêmeo que alimenta a mesma fronteira (`--todo`, que calcula `root`); PoC reproduzido antes e depois, mutation testing em cópia extraída fora da árvore confirmou vermelho→verde. Demais ocorrências de `abspath` do módulo (`TOOLS_DIR`, `_check_run_file`, `checks/chk_frescor.py`, e as duas de `todo_fix.py`) avaliadas e mantidas -- não decidem fronteira contra caminho controlado por quem invoca. Hardlink e TOCTOU ficam declarados em aberto (fora do escopo desta fatia). | Alta | AUD-FINAL | Média | 🔍 Pendente verificação | — |
| REL-1 | W13 | Release | CHANGELOG do zero (referenciando os consertos de 27-28/07 já feitos no toolkit, por descrição de conteúdo, não por SHA -- o histórico foi reescrito mais de uma vez por sanitização de vazamento, e hash antigo citado aqui já não seria estável) + tag **v1.0.0** + release no GitHub. **A tag exige confirmação do líder no contexto.** Entregue em `CHANGELOG.md`. | Alta | AUD-FINAL, SYMLINK-1, README-1 | Baixa | 🔍 Pendente verificação | — |
| MIG-1 | W14 | Migração | Migração da máquina do líder (D-8): `~/.claude/githooks` vira **symlink** para `tools/hooks/` do clone, e a skill instalada passa a apontar para o clone. Passo de verificação obrigatório (`git config --global --get core.hooksPath` + commit de teste em repo sacrificável). Não pode deixar os hooks mortos nem duplicados. | Alta | REL-1 | Média | 🔍 Pendente verificação | — |
| AUD-FUP-1 | W15 | Testes | **Follow-up IMPORTANTE da auditoria**: `_provar_invariantes` (`tools/todo_fix.py`), a rede que decide se e seguro escrever no arquivo do usuario, nao tem teste que prove deteccao de divergencia **so de espaco em branco ou terminador** numa linha preservada -- um mutante que enfraquece a comparacao byte-exata sobrevive a suite inteira. E o ponto cego justamente na classe CRLF/BOM, que o proprio projeto identificou como o Windows da promessa cross-platform. | Média | AUD-FINAL | Baixa | ⏳ Pendente | — |
| AUD-FUP-2 | W15 | Testes | **Follow-up IMPORTANTE da auditoria**: as camadas 1 e 2 do `guard_no_real_fixtures.py` funcionam hoje (comprovado ao vivo pela auditoria) mas **nao tem nenhum teste automatizado** -- so a camada 3 tem. A camada 1 e a unica ativa no CI publico sem configuracao alguma, ou seja, a que mais precisaria de rede de regressao. | Média | AUD-FINAL | Baixa | ⏳ Pendente | — |
| AUD-FUP-3 | W15 | Auditoria | **Follow-up COSMÉTICO da auditoria** (5 achados agrupados): contagem de skip na primeira rodada de cache frio; achados do `ruff` hoje declarados informativos sem config commitada que os torne gate; testes sem marca de licenca; dois `open()` sem encoding explicito no log de frescor; e a nota de que o proprio auditor escreveu nomes reais na primeira versao do relatorio antes de corrigir (nunca commitada). Detalhe em `docs/auditoria/AUD-FINAL-2026-07-28.md`. | Baixa | AUD-FINAL | Baixa | ⏳ Pendente | — |
| WIKI-1 | W15 | Docs | Wiki do repo (GitHub wiki-native) + documentação `.md` extensa em registro didático para **iniciante em computação** (explica todo jargão, passo a passo, sem assumir conhecimento), derivada de `docs/` -- linka, não duplica. Regra da casa: último item, pós-tag, executado por `technical-writer`, **nunca inline**. Entregue: `docs/para-iniciantes.md` (comandos executados e verificados de verdade) + 5 páginas na Wiki nativa do GitHub (Home, Comandos, Auditoria-e-Correcao, Conceitos, Solucao-de-Problemas), publicadas via push no repositório separado da wiki. | Baixa | REL-1 | Alta | 🔍 Pendente verificação | — |
| HOOKDOC-1 | W15 | Docs | Conserto da receita "por projeto" de `tools/README.md`, publicada quebrada na v1.0.0: o shim `post-commit` procura `todo_freshness.py` **um nível acima** de si mesmo, e `todo_freshness.py` importa `todo_lib.py` da **mesma pasta** que ele -- a receita antiga copiava os três arquivos juntos para dentro de `.githooks/`, quebrando as duas exigências. Corrigido preservando a estrutura de dois níveis do próprio `tools/` (`tools/hooks/` + `tools/`) dentro do consumidor: shims em `.githooks/hooks/`, os dois scripts Python em `.githooks/` (um nível acima). Os shims em si **não** foram alterados (caminho relativo correto para o layout do repositório). Verificado ao vivo com `git commit` de verdade num repositório descartável, tanto para a receita "por projeto" quanto para a "global" (nenhuma das duas tinha sido testada antes). Teste automatizado novo (`tests/test_install_recipe_e2e.py`, 3 casos) exercita as duas receitas via hook real, não só chamando o script Python direto. `docs/para-iniciantes.md` e a Wiki (`Solucao-de-Problemas`) atualizados para a receita corrigida, removendo o contorno documentado antes. | Alta | — | Média | 🔍 Pendente verificação | — |
| PRED-FALLBACK | W15 | Consertos | Reconsideração do fallback de tabela legada dos predicados de status (`tools/todo_lib.py`), pedida em um relato interno (não versionado) pela sessão de um consumidor (medição de 80 casos contra o toolkit anterior, 4 divergentes). Trocado o fallback "contém a palavra X?" por-predicado por uma única função de decisão (`_classify_legacy`) com **precedência por posição**: o vocabulário canônico de menor índice na célula vence, com os compostos (`pendente design`, `pendente verificação`) casados antes da regra de posição para não ressuscitar o BUG-5 grave. Resolve a incoerência `'Concluído e verificado'` (saía com `is_done` e `is_awaiting_verification` ambos `True`) e os 3 falsos-negativo medidos (`'Pendente (verificar disponibilidade)'`, `'Em andamento (verificar com o time)'`, `'A verificar'`). Camada de emoji intocada (prova por diff). Suíte antiga inteira (447 casos) preservada; asserção de `tests/test_bug5_predicados_emoji.py:58` trocada com comentário datado. | Alta | BUG-5 | Baixa | 🔍 Pendente verificação | — |
| TAB-SOT-007 | W16 | Distribuição | Detector de drift do pin de submódulo (`tools/submodule_pin_drift.py`), nascido de falha objetiva medida em 16/08: o gitlink de `skills/tab_pendencias` no `claude-memory` ficou **67 commits atrás** da revisão publicada sem que nada acusasse, e um `clone --recursive` restaurava a skill **sem o toolkit inteiro** (81 arquivos, 17.250 linhas). Read-only e warning-only: lê o gitlink por `git ls-tree HEAD` (não `git submodule status`, que inspeciona o checkout e falha se o submódulo nunca foi inicializado), consulta o remoto por `ls-remote` e **nunca** `fetch`, e **sem rede declara `nao_verificavel`, jamais `atualizado`**. Qualquer sinal de comparação falso vence sinal indeterminado, para drift confirmado não ser mascarado por incerteza. Ordena release por semver numérico e resolve tag anotada pela linha peeled. Caminho e URL são parâmetros. 24 testes em repos git fabricados. **Gêmeo CI entregue como snippet documentado e NÃO ligado**: ligar pertence ao `claude-memory`. | Alta | — | Média | 🔍 Pendente verificação | — |
| PRED-FALLBACK-2 | W16 | Consertos | Defeito de integridade de dado na v1.0.2, achado por equivalência diferencial contra a cópia legada em `githooks.pre-v1.0.0`. O `is_flip_eligible` devolvia verdadeiro para `Pendente-design`, `Pendente depois design`, `andamento design` e `andamento depois design`, violando a regra dura D-1 de que item de design nunca flipa. Como o consumidor é o `todo_sync.py`, que **escreve** na tabela do usuário, o item seria promovido sozinho em silêncio. Causa-raiz: o `PRED-FALLBACK` implementou o composto como adjacência de string com radical fixo, e a forma adjacente exata passava por ser a única com teste. Trocado por **co-ocorrência restrita** de radical mais qualificador, sem adotar a prioridade incondicional do legado (que ressuscitaria a incoerência de `Concluído e verificado`). Gêmeos consertados em `Pendente verificação` e no radical `andamento`. Matriz de 216 combinações: 90 divergentes antes, 0 depois. | Alta | PRED-FALLBACK | Média | 🔍 Pendente verificação | — |
| TAB-ADD-001 | W17 | Intake | Núcleo mecânico de `--add` (`tools/todo_intake.py`): `WorkCandidate` + cascata ADR-0002 (d) + dedup por ID exato + persistência L0 (append puro com marcador `<!-- intake:id -->`) + residual INBOX com metadado de triagem + journal write-ahead + pré-condição de árvore limpa + abort se INBOX classificável (drain-first parcial). Apply L0/residual/DUPLICATE. Seção `--add` no corpo do `SKILL.md` (sem alterar `description` do frontmatter). | Alta | — | Alta | 🔍 Pendente verificação | — |
| TAB-ADD-005 | W17 | Intake | SCOPED_REORDER apply: menor subgrafo seguro S, raw byte-a-byte fora de S, limite `scoped_reorder_max_fraction` (default 0.5 / env / ini), promoção a FULL se fração ou multi-Grupo; journal só após build ok. | Alta | TAB-ADD-001 | Alta | 🔍 Pendente verificação | — |
| TAB-ADD-006 | W17 | Intake | FULL_REORDER apply: topo estável + ondas W1.., W1 Status intacto, marker intake; ciclo aborta sem escrita e sem journal órfão. | Alta | TAB-ADD-001 | Alta | 🔍 Pendente verificação | — |
| TAB-ADD-007 | W17 | Intake | Strip residual INBOX do mesmo `item_id` após integração bem-sucedida (L0/SCOPED/FULL) e em DUPLICATE se residual coexistir com a tabela; P-dup mecânico só por ID exato na tabela canônica -- ID só na residual **não** é DUPLICATE, é strip+reentrada pós-decisão do líder; skill documenta re-chamada do intake com flags de julgamento e que 2–3 opções são dever do agente. | Alta | TAB-ADD-001 | Baixa | 🔍 Pendente verificação | — |
| INTAKE-AGE-1 | W17 | Arquitetura | Especificar o **predicado de envelhecimento** da INBOX residual, lacuna declarada no ADR-0002 e criada pela decisão do líder de política de apresentação **híbrida** (nem automática em todo início de sessão, nem só sob demanda: a decisão pendente aparece quando envelhece). O ADR previa só as duas opções binárias, então este mecanismo não foi desenhado. Definir (a) o predicado exato, (b) onde ele é avaliado, e (c) **como o contador de ciclos avança sem travar quando nenhuma sessão roda o dreno** -- se `cycles` só incrementa ao drenar e ninguém drena, o item nunca envelhece e a forcing function nunca dispara, que é a mesma falha de liveness que a campanha ataca reaparecendo dentro do mecanismo que deveria resolvê-la. Encaminhado formalmente pelo líder em 16/08. | Alta | — | Média | ⏳ Pendente | — |
| PYFLOOR-2 | W18 | Distribuição | **Decisão do líder em 16/08: subir o piso oficial de Python para 3.11** e passar a declará-lo. O piso anterior era promessa sem lastro: não declarado, sem teste, e a matriz de CI só exercitava versões acima dele. Declarar em metadado do pacote, documentar no README, e conferir que a matriz cobre o piso. A razão original de escolher config em INI era justamente evitar exigir 3.11+; o formato **fica como está** por decisão do orquestrador, porque trocar quebraria config de quem já usa e o ganho seria nulo. A justificativa do formato passa a ser histórica. | Alta | — | Baixa | ⏳ Pendente | — |
| MDASH-2 | W18 | Governança | **Decisão do líder em 16/08: abrir exceção explícita do hook anti-travessão para este repositório**, com o motivo escrito. O travessão é o valor canônico de célula vazia no schema do produto, então o hook proibia o produto de mostrar literalmente o próprio contrato, e o README contornava descrevendo o símbolo em prosa. A exceção vale só aqui; a proteção segue nos demais projetos. | Média | — | Baixa | ⏳ Pendente | — |
| HOOKSRC-1 | W18 | Distribuição | **Decisão do líder em 16/08: os ganchos vivos passam a rodar da instalação PUBLICADA**, não da árvore de desenvolvimento. Hoje `~/.claude/githooks` é symlink para o checkout de dev, então código não commitado executa a cada commit do líder em qualquer projeto da máquina. Apontar para o submódulo, que só carrega revisão publicada e testada. Custo aceito: mudança em gancho passa a exigir publicar e avançar o pin para valer. | Alta | — | Média | ⏳ Pendente | — |
| VERB-STATUS-2 | W18 | Consertos | **Decisão do líder em 16/08: célula de status legada contendo só o verbo, sem vocábulo canônico, NÃO classifica -- e a auditoria passa a apontá-la.** Nunca agir errado, nunca ficar calada. Hoje o produto lê `Verificar disponibilidade` como aguardando verificação, e quem consome isso é a rotina que avança status sozinha na tabela do usuário. Decidir por análise de verbo assumiria português no conteúdo livre, o que o requisito canônico proíbe. | Alta | — | Média | ⏳ Pendente | — |
| SKILL-DESC-2 | W18 | Docs | **Decisão do líder em 16/08: escrever uma TERCEIRA redação** do resumo que aciona a skill. A reescrita parada na árvore encurta o texto e inverte o destaque entre os comandos; o pedido é ficar com o ganho (anunciar o comando de correção) sem o risco (mudar ordem de destaque sem medir efeito no acionamento). Fecha o `SKILL-DESC-1` e limpa a última alteração não publicada da árvore. | Média | — | Baixa | ⏳ Pendente | — |
| FIX-ESCOPO-2 | W18 | Docs | **Decisão do líder em 16/08: confirmada retroativamente a entrega de DUAS classes de correção automática**, e não as quatro do desenho original. As duas ausentes movem linhas, e mover linha em arquivo de terceiro é a operação de maior risco do produto. Ajustar ADR e README para descreverem o escopo real, não o pretendido. Regra que fica: a auditoria nunca marca defeito como auto-corrigível sem o corretor existir. | Média | — | Baixa | ⏳ Pendente | — |
| MIG-DIFF | W16 | Distribuição | **Resolvido em 16/08.** Exigia provar que a versão do repo era superset comportamental da cópia legada antes de removê-la. Provado por equivalência diferencial executável: 816 comparações finais entre as duas versões, restando 2 divergências, ambas do fix intencional anterior onde o produto está certo. A cópia legada foi removida do vault por decisão do líder. Um achado da prova virou o `VERB-STATUS-2`. | Alta | — | Média | 🔍 Pendente verificação | — |
| TAB-WSJF-001 | W19 | WSJF | Régua única Fibonacci `(1,2,3,5,8,13,20)` em `tools/wsjf.py` (`normalize_score` reject/snap); corrige a dupla régua 1-20 linear vs fib do `SKILL.md`/AGILE. | Alta | TAB-ADD-006 | Média | 🔍 Pendente verificação | — |
| TAB-WSJF-002 | W19 | WSJF | Profiles `early` (default) e `safe` (SAFe formal): early aceita rótulos via `label_to_fib`; safe exige ints fib. Config INI `[wsjf]`. | Média | TAB-WSJF-001 | Baixa | 🔍 Pendente verificação | — |
| TAB-WSJF-003 | W19 | WSJF | Topologia antes de WSJF: `topology_before_wsjf` + `order_levels_then_wsjf` -- nivel-primeiro; WSJF só dentro do nivel; dependente nunca fura prereq. | Alta | TAB-WSJF-001 | Média | 🔍 Pendente verificação | — |
| TAB-WSJF-004 | W19 | WSJF | Scoring local sem repriorizar o universo: peers só via `peer_scores` (nunca auto-mapear Prioridade da tabela); L0/SCOPED sem WSJF. | Alta | TAB-WSJF-001 | Média | 🔍 Pendente verificação | — |
| TAB-WSJF-005 | W19 | WSJF | Estabilidade: `stable_rank_within_level` com epsilon comparável, empate estável, pin de `🔄` WIP, determinismo. | Alta | TAB-WSJF-003 | Média | 🔍 Pendente verificação | — |
| TAB-WSJF-006 | W19 | WSJF | Explicabilidade: `explain_move` (3 linhas ITEM/causa/input_material) no report do FULL só para movimento material de pos/onda. | Média | TAB-WSJF-003 | Baixa | 🔍 Pendente verificação | — |
| TAB-WSJF-007 | W19 | WSJF | Criticidade factual: `source=bus` ignora rótulos retóricos; só ints explícitos pontuam. Integração FULL em `todo_intake.py`. | Alta | TAB-WSJF-001 | Média | 🔍 Pendente verificação | — |

## INBOX (descobertas não priorizadas)

<!-- 1 linha por descoberta: `- <ID tentativo ou —>: descrição curta`. Drenada por --create/--reorder. -->

- FIX-RISCO-1: riscos conhecidos do `--fix`, declarados pelo implementer e **não** resolvidos: (a) dois `--fix --apply` simultâneos no mesmo repo -- ambos veriam árvore limpa, o segundo `os.replace` venceria atomicamente e o primeiro não saberia que foi sobrescrito; (b) janela TOCTOU inerente a qualquer fluxo ler-modificar-escrever sem lock de sistema operacional (a pré-condição de árvore limpa mitiga, não elimina); (c) no Windows, `os.replace` sobre destino somente-leitura pode divergir do POSIX -- coberto por `except OSError` genérico, mas **sem prova empírica**, e a matriz de CI não exercita esse caminho.

---

## Notas de montagem (o que foi fundido, cortado e por quê)

Registrado aqui porque **truncar em silêncio faz a tabela parecer completa quando não é**.
Gate anti-OE decidido por Cósimo (Chief of Staff): **thread direta**, não o time de 5 lentes --
o `--create` é artefato mecânico de planejamento e as decisões já estavam fechadas. A única
exceção que ele exigiu é o passe de ADR (`ADR-1`) antes dos consertos.

**Fundidos** (viraram sub-bullets dentro de um item, não sumiram): `LIC-1` → `SCAF-1`;
`ABS-2` → `ABS-1`; `CI-2`, `CI-3`, `TST-T2` (estática), `TST-T8` (secrets) e `TST-GAP-3`
(shims) → jobs do `CI-1`; `CIT-1`+`TCH-1`+`HDR-1` → `PRED-FIX`; `CHK-01..04`+`CHK-08`+`CHK-11`
→ `CHK-CORE`; `CHK-05..07` → `CHK-GRAPH`; `CHK-12`+`CHK-13`+`CHK-14` → `CHK-CASA`;
`FIX-SUGGEST` → `FIX-ENG`; `CHANGELOG-1`+`TAG-1` → `REL-1`.

**Cortados, com o que os substitui**: `TST-T5` (deps), `TST-T12` (CVE) e `AUD-DEPS` -- o runtime
é stdlib puro, não há superfície; substituídos por um guard de 3 linhas no `CI-1` ("nenhum import
fora da stdlib") e pela verificação de licença dentro do `AUD-FINAL`. `TST-T14` (integração) --
já coberto por `GAP-2` + `CONTR-1`. `AUD-DISC/ARCH/SEC/QUALITY/COV/LANG/REPORT` -- 7 itens
consolidados em `AUD-FINAL` (o conteúdo permanece; o que sumiu foi a burocracia de 7 linhas).
`CI-4` (consistência README×SKILL.md) -- vira linha do DoD de `README-1`, porque o README só
nasce em W10 e um check sem objeto é infra vazia. `CHK-15..18` (`--audit=repo`) -- **v1.1** por
decisão D-5; higiene de disco fica fora da skill.

**Ondas**: 14 ondas para 33 itens (32 pendentes + 1 concluído) porque as dependências são reais,
não decorativas. Itens da mesma onda são paralelizáveis **logicamente**; quando dois deles tocam
o mesmo arquivo (caso de W5, toda ela em `todo_lib.py`), o orquestrador serializa fatia a fatia --
disputa de arquivo não é dependência, mas impede paralelismo de verdade. WIP = 1 onda.

**Verificação desta tabela** (rodada na montagem, com script escape-aware -- é o que `CHK-05/06/07`
vão automatizar): 33 linhas de 9 células, **zero ID duplicado**, **zero pré-requisito inexistente**,
**zero ciclo**. Um defeito real foi encontrado e corrigido aqui: `AC-FIX` estava na mesma onda do
seu pré-requisito `FIX-ENG` -- exatamente o `CHK-07`, achado na primeira aplicação. O
`todo_health.py` não roda ainda porque `Projects/tab_pendencias/` não é repositório git até o
`SCAF-1`; ele saiu com a mensagem "Nao e um repositorio git" **e exit code 0**, que é o `CLI-1`
se manifestando ao vivo.

**WSJF qualitativo** (porte early; a tabela de scoring completa da [[AGILE]] §17.2 é exigida em
scale/bigtech, aqui seria cerimônia). O critério que ordenou: fundação e one-way-door primeiro
(`ADR-1`, `SCAF-1` com a licença), **rede de segurança antes dos consertos** (`CONTR-1` precede
todo conserto de parser de propósito -- é o que prova 116=116 e 215=215 depois de mexer), e
desbloqueio de terceiro cedo (`BUS-1` sobe assim que `BUG-5` fecha, porque o consumidor B está
parado esperando).

**Não pode ser cortado** (Cósimo, risco de subdimensionar): `CORP-0` e `CONTR-1` (sem eles não
há contrato, há esperança); os invariantes de round-trip byte-exato e "sync nunca seta ✅"
(quebrados, corrompem TODO.md de terceiro); `ENC-1`; o gate `AC-FIX`; e o mutation testing por
`qa-engineer` independente -- implementer ≠ reviewer não é cerimônia, é o único mecanismo que
pega teste que passa sem verificar nada.

**Ordem inviolável respeitada**: T1 unitário ride com a implementação e nunca vira item;
`TST-T15` e `AUD-FINAL` são downstream do que cobrem. Nota de dogfooding: os itens de lacuna de
teste foram nomeados `GAP-*`, não `TST-*`, justamente para não colidirem com o namespace do
catálogo e disparar um falso positivo do futuro `CHK-12`.

⚠️ **Hook de TDD**: este projeto ainda não tem `.claude/tdd-guard.json`. O TDD dos itens de
implementação depende de disciplina do implementer até o hook ser ativado.
