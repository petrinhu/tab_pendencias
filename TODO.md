# TODO -- tab_pendencias v2 (skill + toolkit distribuíveis)

> Tabela canônica de pendências. **A ordem das linhas É a ordem de execução recomendada**;
> a coluna `Onda` marca os passos de igual valor que podem rodar em paralelo.
> Fonte dos fatos: `prompt_inicial.md`. Decisões fechadas: `decisoes_lider.md`.
> Manuais: `TESTES.md`, `AUDITORIAS.md`. Porte: **early / Pipeline-Lean**, kanban, **WIP = 1 onda**.
>
> **Dogfooding**: este arquivo usa a própria skill que o projeto constrói (schema de 9 colunas,
> INBOX, ondas de teste/auditoria downstream, item de Wiki como última onda pós-tag).

## Notas de montagem (o que foi fundido, cortado e por quê)

Registrado aqui porque **truncar em silêncio faz a tabela parecer completa quando não é**.
Gate anti-OE decidido por Cósimo (Chief of Staff): **thread direta**, não o time de 5 lentes --
o `--create` é artefato mecânico de planejamento e as decisões já estavam fechadas. A única
exceção que ele exigiu é o passe de ADR (`ADR-1`) antes dos consertos.

**Fundidos** (viraram sub-bullets dentro de um item, não sumiram): `LIC-1` → `SCAF-1`;
`ABS-2` → `ABS-1`; `CI-2`, `CI-3`, `TST-T2` (estática), `TST-T8` (secrets) e `TST-GAP-3`
(shims) → jobs do `CI-1`; `CIT-1`+`TCH-1`+`HDR-1` → `PRED-FIX`; `CHK-01..04`+`CHK-08`+`CHK-11`
→ `CHK-CORE`; `CHK-05..07` → `CHK-GRAPH`; `CHK-12`+`CHK-13`+`CHK-14` → `CHK-CASA`;
`FIX-SUGGEST` → `FIX-ENG`; `CHANGELOG-1`+`TAG-1` → `REL-1`.

**Cortados, com o que os substitui**: `TST-T5` (deps), `TST-T12` (CVE) e `AUD-DEPS` -- o runtime
é stdlib puro, não há superfície; substituídos por um guard de 3 linhas no `CI-1` ("nenhum import
fora da stdlib") e pela verificação de licença dentro do `AUD-FINAL`. `TST-T14` (integração) --
já coberto por `GAP-2` + `CONTR-1`. `AUD-DISC/ARCH/SEC/QUALITY/COV/LANG/REPORT` -- 7 itens
consolidados em `AUD-FINAL` (o conteúdo permanece; o que sumiu foi a burocracia de 7 linhas).
`CI-4` (consistência README×SKILL.md) -- vira linha do DoD de `README-1`, porque o README só
nasce em W10 e um check sem objeto é infra vazia. `CHK-15..18` (`--audit=repo`) -- **v1.1** por
decisão D-5; higiene de disco fica fora da skill.

**Ondas**: 14 ondas para 33 itens (32 pendentes + 1 concluído) porque as dependências são reais,
não decorativas. Itens da mesma onda são paralelizáveis **logicamente**; quando dois deles tocam
o mesmo arquivo (caso de W5, toda ela em `todo_lib.py`), o orquestrador serializa fatia a fatia --
disputa de arquivo não é dependência, mas impede paralelismo de verdade. WIP = 1 onda.

**Verificação desta tabela** (rodada na montagem, com script escape-aware -- é o que `CHK-05/06/07`
vão automatizar): 33 linhas de 9 células, **zero ID duplicado**, **zero pré-requisito inexistente**,
**zero ciclo**. Um defeito real foi encontrado e corrigido aqui: `AC-FIX` estava na mesma onda do
seu pré-requisito `FIX-ENG` -- exatamente o `CHK-07`, achado na primeira aplicação. O
`todo_health.py` não roda ainda porque `Projects/tab_pendencias/` não é repositório git até o
`SCAF-1`; ele saiu com a mensagem "Nao e um repositorio git" **e exit code 0**, que é o `CLI-1`
se manifestando ao vivo.

**WSJF qualitativo** (porte early; a tabela de scoring completa da [[AGILE]] §17.2 é exigida em
scale/bigtech, aqui seria cerimônia). O critério que ordenou: fundação e one-way-door primeiro
(`ADR-1`, `SCAF-1` com a licença), **rede de segurança antes dos consertos** (`CONTR-1` precede
todo conserto de parser de propósito -- é o que prova 116=116 e 215=215 depois de mexer), e
desbloqueio de terceiro cedo (`BUS-1` sobe assim que `BUG-5` fecha, porque o consumidor B está
parado esperando).

**Não pode ser cortado** (Cósimo, risco de subdimensionar): `CORP-0` e `CONTR-1` (sem eles não
há contrato, há esperança); os invariantes de round-trip byte-exato e "sync nunca seta ✅"
(quebrados, corrompem TODO.md de terceiro); `ENC-1`; o gate `AC-FIX`; e o mutation testing por
`qa-engineer` independente -- implementer ≠ reviewer não é cerimônia, é o único mecanismo que
pega teste que passa sem verificar nada.

**Ordem inviolável respeitada**: T1 unitário ride com a implementação e nunca vira item;
`TST-T15` e `AUD-FINAL` são downstream do que cobrem. Nota de dogfooding: os itens de lacuna de
teste foram nomeados `GAP-*`, não `TST-*`, justamente para não colidirem com o namespace do
catálogo e disparar um falso positivo do futuro `CHK-12`.

⚠️ **Hook de TDD**: este projeto ainda não tem `.claude/tdd-guard.json`. O TDD dos itens de
implementação depende de disciplina do implementer até o hook ser ativado.

## INBOX (descobertas não priorizadas)

<!-- 1 linha por descoberta: `- <ID tentativo ou —>: descrição curta`. Drenada por --create/--reorder. -->

---

| ID | Onda | Grupo | Descrição Técnica | Prioridade | Pré-requisito | Dificuldade | Status | Estado Auditado |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| W0-DEC | — | Governança | Kickoff `/bigtech`: porte early classificado por Cósimo, mapa de ativação aprovado, e as 8 decisões **D-1..D-8** resolvidas pelo líder (registro em `decisoes_lider.md`). Gate anti-OE do `--create` decidido por Cósimo: **thread direta** + 1 passe de ADR. | Alta | — | Baixa | ✅ Concluído | — |
| ADR-1 | W1 | Arquitetura | ADR da fronteira **núcleo genérico × convenções da casa** (nenhum check do núcleo pode depender do perfil "casa"), do contrato do parser (round-trip byte-exato, compat 8/9 colunas) e dos limites de `--audit`/`--fix`. Executor: `software-architect`. É o risco arquitetural real do projeto e precisa existir ANTES dos consertos, não como auditoria tardia. Entregue em `docs/adr/0001-*.md`; gerou as decisões D-9/D-10/D-11. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | — | Média | 💡 Decisão tomada | — |
| SCAF-1 | W1 | Scaffold | Clonar o repo em `Projects/tab_pendencias/`, montar o layout monorepo da D-4 (`tools/`, `tools/hooks/`, `tests/`, `references/`) preservando os 21 commits, **e relicenciar o repo inteiro para GPL-3.0-or-later** (D-3): trocar `LICENSE`, cabeçalhos dos fontes, purgar menção residual à PolyForm no README/SKILL.md. Publicação de licença é irreversível na prática. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | — | Média | 💡 Decisão tomada | — |
| ABS-1 | W2 | Scaffold | Absorver de `~/.claude/githooks/`: os 4 scripts (`todo_lib`, `todo_sync`, `todo_health`, `todo_freshness`) para `tools/`, e `_chain.sh` + os 7 shims POSIX para `tools/hooks/`; mover a suíte pytest (**41 testes**) para `tests/` e deixá-la 100% verde no novo layout. Atômico: absorver os `.py` sem os testes deixa o repo vermelho. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | SCAF-1 | Média | 💡 Decisão tomada | — |
| DOC-FRESH | W2 | Docs | Internalizar em `references/` a parte NORMATIVA das regras de frescor (D-2), hoje só em `~/.claude/docs/tabela-pendencias-frescor.md`; o vault fica com o histórico/racional. Sem isto, quem clona não recebe a norma que a skill exige. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Média | SCAF-1 | Baixa | 💡 Decisão tomada | — |
| CONTR-1 | W3 | Testes | Testes de **contrato** com as fixtures reais (consumidor A **116** itens, consumidor B **215**), mantidas apenas LOCALMENTE e nunca commitadas sem anonimização. Vem ANTES dos consertos de propósito: é a rede de segurança que prova que nenhuma mudança quebra os consumidores vivos. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | ABS-1 | Baixa | 💡 Decisão tomada | — |
| CORP-0 | W3 | Testes | Corpus **sintético** de aceitação (AC-0): outra língua, outro esquema de ID (`#NN`, `XYZ.9.9`), outra estrutura de seções; property-based sobre IDs gerados. Cap anti-OE do Cósimo: **no máximo 2 alfabetos e 2 formatos de ID**. É o que prova que a skill é agnóstica a projeto. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | ABS-1 | Média | 💡 Decisão tomada | — |
| CI-1 | W3 | CI | Workflow único do GitHub Actions com jobs: pytest em **matrix ubuntu + windows** (a promessa de Git-for-Windows não se sustenta só com ubuntu), `shellcheck` + smoke dos shims, lint de Markdown, `gitleaks` (secrets), guard "**nenhum import fora da stdlib**" e guard **anti-vazamento de fixture real** (repo público). Actions pinadas por SHA. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | ABS-1 | Média | 💡 Decisão tomada | — |
| CI-2 | W4 | CI | Ampliar a matrix de pytest do `CI-1` para **Debian, Fedora e Arch** via `container:` sobre `ubuntu-latest` (sem GitHub runner nativo dessas distros), somando-se a ubuntu+windows nativos (não os substitui). Imagens pinadas por tag+digest (nunca `latest` solto). Smoke dos shims também sob o `/bin/sh` do container Debian. Validado localmente com `podman` (as 3 imagens, pytest completo + smoke). Achados registrados no próprio workflow: (1) Ubuntu **já** usa dash como `/bin/sh` (a premissa original de "1ª exposição a shell estrito" estava errada, corrigida in-loco); (2) piso/teto de Python medido = Debian 3.11.2, Fedora 3.13.9, Arch 3.14.6; (3) jobs de container rodam como **root** por padrão, o que produz falhas espúrias em testes sensíveis a permissão (chmod) — não corrigido nesta fatia (privilege-drop portátil entre apt/dnf/pacman é decisão maior), apenas documentado no job. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Média | CI-1 | Média | 💡 Decisão tomada | — |
| SANIT-1 | W4 | Segurança | **Incidente de privacidade (28/07)**: os arquivos internos de planejamento foram versionados num repo PÚBLICO por erro de orquestração, expondo caminhos da máquina do autor, nomes de projetos privados e o bus interno. Conserto: tirar os 3 arquivos do versionamento (mantidos em disco, no `.gitignore`), designação neutra "consumidor A/B" nos docs, caminhos de fixture vindos de variável de ambiente, e redesenho do guard anti-vazamento (que continha os próprios termos que deveria proteger). Histórico reescrito com `filter-repo` + force-push, autorizado pelo líder; verificado por clone fresco do remoto. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | CI-1 | Média | 💡 Decisão tomada | — |
| BUG-5 | W4 | Consertos | Classificação de status por **emoji-prefixo** (D-1), com fallback word-boundary só para tabela legada sem emoji + aviso no `--audit`. Hoje `is_awaiting_verification` casa "VERIFICADO" e `is_done` casa "inconclusivo" (`todo_lib.py:169-174`); cobre também SUB-1 (`"🔴 Bloqueado (dependente de X)"` → pendente) e SUB-2 (`"⏳ Pendente (verificar disponibilidade)"` → bloqueia flip legítimo). Afeta todos os repos do líder. **CANCELADO 2026-08-16 (legado / anti-OE):** supersedido pelo toolkit v1.2 / predicados emoji-prefixo + testes; residual de verificação formal se necessário vira item novo. Funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | ABS-1, ADR-1, CONTR-1 | Média | 💡 Decisão tomada | — |
| CLI-1 | W4 | Consertos | Contrato de CLI da D-6 nos 3 scripts: `argparse` + `--help`; exit `0`=ok, `1`=erro de execução, `2`=achados; **flag desconhecida = erro** (hoje `--aply` é ignorado em silêncio e `todo_sync.main` devolve 0 até quando nem é repo git). **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Média | ABS-1 | Média | 💡 Decisão tomada | — |
| GAP-1 | W4 | Testes | Suíte do `todo_health.py` do zero -- é o único módulo hoje sem nenhum teste. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Média | ABS-1 | Baixa | 💡 Decisão tomada | — |
| GAP-2 | W4 | Testes | Teste e2e de `todo_freshness.main()` (o caminho real, com `diff-tree`), hoje sem cobertura nenhuma. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Média | ABS-1 | Média | 💡 Decisão tomada | — |
| HOOKISO-1 | W5 | Testes | Isolar os testes que fazem `git init` do `core.hooksPath` **GLOBAL** desta máquina (`~/.gitconfig` → `~/.claude/githooks/`): sem proteção, todo commit num repositório temporário de teste dispara o hook AMBIENTE de verdade por baixo dos panos, poluindo `$GIT_DIR/todo-freshness.log` e quebrando a determinação/isolamento da suíte. Helper `git_init_isolado` (config `core.hooksPath` **local**, nunca `--global`) centralizado em `tests/conftest.py`; aplicado em `test_cli_contract.py` (que tinha o gêmeo do achado, sem proteção nenhuma) com 2 testes de prova. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Média | CLI-1 | Baixa | 💡 Decisão tomada | — |
| SPRAWL-1 | W5 | Consertos | Fim explícito da tabela canônica, revisado por **D-12**: a tabela ATRAVESSA um heading quando o que vem depois é continuação (mesmo schema); ENCERRA ao encontrar 2º cabeçalho ID+Status, tabela de schema diferente (mesmo com ncols coincidente), ou fim do documento. `CONTR-1` verde: 116=116, 215=215 medidos pós-conserto. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | ABS-1, ADR-1, CONTR-1 | Média | 💡 Decisão tomada | — |
| BUG-1' | W5 | Consertos | ID duplicado passa em silêncio: `parse_status_map` deixa a última linha vencer (`todo_lib.py:144-147`) e `by_id` idem (`todo_sync.py:122`); nenhum teste detecta. A instância foi removida do dado, a CLASSE segue aberta. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | ABS-1 | Baixa | 💡 Decisão tomada | — |
| PRED-FIX | W5 | Consertos | Três predicados de uma linha, um commit citando os 3 IDs: **CIT-1** `cited_ids("fechei V-12.")` → `[]` (ponto final cai no lookahead, `todo_lib.py:177-185`); **TCH-1** `touched_code(["src/inbox/parser.py"])` → False (substring `/inbox/` em path de código, `todo_lib.py:49-62`) -- restringir ao `inbox/` do topo; **HDR-1** `_is_header` dispara com qualquer célula contendo "status" (`todo_lib.py:100-102`). **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | ABS-1 | Baixa | 💡 Decisão tomada | — |
| ENC-1 | W5 | Consertos | Unificar a política de exceção/encoding: `todo_freshness.main` engolia QUALQUER exceção de leitura (`todo_freshness.py:81`) enquanto `todo_sync`/`todo_health` crashavam cru em `UnicodeDecodeError`; agora `todo_freshness` continua exit 0 (warn-only) mas não mais em silêncio (stderr + log `erro=leitura_todo:<Tipo>`), e `todo_sync`/`todo_health` viram mensagem clara + exit 1, com flag `-v/--verbose` (traceback completo) nos 3 scripts. BOM/CRLF exercitados ponta-a-ponta. Não é polimento: **CRLF/BOM é o Windows do claim cross-platform**. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | ABS-1 | Baixa | 💡 Decisão tomada | — |
| ROOT-1 | W5 | Consertos | `todo_freshness.py` chamava `git diff-tree --no-commit-id --name-only -r HEAD` **sem `--root`**: no commit RAIZ de um repositório (sem pai) esse comando devolve saída VAZIA, então `touched_code(files)` era sempre `False` ali e o aviso "tocou código mas não citou ID" nunca disparava no primeiro commit de um repo. Consertado com `--root`. `todo_sync.py` usa `git log --name-only` (não `diff-tree`) para o mesmo propósito, e `git log` já mostra os arquivos do commit raiz sem precisar de `--root` -- confirmado ao vivo; **não tinha o mesmo bug**. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | ABS-1 | Baixa | 💡 Decisão tomada | — |
| AUDIT-ENG | W6 | Audit | `tools/todo_audit.py`: motor dos checks + CLI própria (D-6). Relatório numerado por check com severidade CRÍTICO/IMPORTANTE/COSMÉTICO, evidência `linha:`, marcação **[auto-fixável]** vs **[julgamento]**, saída legível no chat + arquivo opcional em scratchpad (nunca no repo), e a regra **"no silent caps"**: todo descarte ou limitação do próprio audit é declarado no relatório. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | CLI-1, SPRAWL-1, BUG-5 | Alta | 💡 Decisão tomada | — |
| BUS-1 | W6 | Relay | Relay ao consumidor B pelo bus interno, thread `higiene-repo`: comunicar decisao **D-1** (emoji-prefixo) e o estado do toolkit. **Entregue 16/08/2026:** mensagem no bus (commit c284ede, remoto do bus do consumidor B). Bloco espelho em docs/auditoria/BUS-1-relay-D1.md. | Alta | BUG-5 | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| CHK-CORE | W7 | Audit | Integridade de tabela e vocabulário: **CHK-01** ID duplicado (listar as N linhas com diff resumido); **CHK-02** nº de células ≠ ncols, com DIAGNÓSTICO da causa provável (pipe cru? célula faltando? fragmento truncado?) e conserto sugerido; **CHK-03** múltiplos cabeçalhos ID+Status + relatório do SPAN da canônica; **CHK-04** ncols divergente entre tabelas (pré-condição de consolidação); **CHK-08** status fora do vocabulário; **CHK-11** reconciliar o total do `todo_health` com contagem independente (ele já reportou 14 num arquivo de 215 sem avisar). **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | AUDIT-ENG | Alta | 💡 Decisão tomada | — |
| CHK-GRAPH | W7 | Audit | Um walk no grafo de dependências: **CHK-05** pré-requisito citando ID inexistente; **CHK-06** ciclo (reportar o ciclo inteiro); **CHK-07** Onda inconsistente (item na mesma onda de um pré-requisito seu, ou posicionado antes dele na ordem das linhas). **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | AUDIT-ENG | Média | 💡 Decisão tomada | — |
| CHK-09 | W7 | Audit | Claims obsoletas na Descrição ("não pushado", "commit local", "branch X") **verificadas contra o git real** (`git ls-remote`, `cat-file -e`, `diff main --`). Padrões configuráveis com defaults pt+en -- nenhum check pode assumir português no conteúdo livre. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Média | AUDIT-ENG | Média | 💡 Decisão tomada | — |
| CHK-10 | W7 | Audit | Rodar `todo_sync.py` **sem** `--apply` e anexar a proposta ao relatório, com o aviso canônico (citar ID ≠ entregar; ler com desconfiança) e o estado do baseline `todo-sync-ref`. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Média | AUDIT-ENG | Baixa | 💡 Decisão tomada | — |
| AUDIT-PATH | W7 | Audit | `--todo <caminho>`: audita um `TODO.md` fora do cwd/repositório corrente (achado do team-lead ao tentar medir as fixtures de aceitação -- sem isto `AC-REAL` é impossível, pois as fixtures nunca podem ser copiadas para dentro do repo público). Restrição deliberada: o arquivo precisa se chamar exatamente `TODO.md` (mesma convenção de `todo_lib.find_todo`), para `CHK-11` (que re-descobre `root/TODO.md` por caminho de código independente) não comparar contra o arquivo errado. Quando o alvo está fora de qualquer repositório git resolvível, o motor declara isso uma única vez (no silent caps) em vez de deixar `CHK-09`/`CHK-10` degradarem achado a achado sem explicação sistêmica. Read-only preservado e provado por hash nas duas fixtures reais. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | AUDIT-ENG | Média | 💡 Decisão tomada | — |
| GUARD-FP | W7 | CI | Falso positivo do guard "nenhum import fora da stdlib" (`CI-1`): `tools/todo_audit.py` importa `checks.chk_graph`/`chk_frescor`/`chk_core` -- pacote **deste próprio repositório** (`tools/checks/`), não dependência externa -- e o guard antigo só reconhecia stem de arquivo solto como módulo local, nunca exercitado contra um subpacote real. Conserto: resolução estrutural componente-a-componente (pacote com `__init__.py` ou módulo `.py`, igual à busca do importador do Python), não lista/stem fixo -- essa é a mesma classe de deriva de lista fixa já vista noutros guards. Provado com import de terceiro real plantado em cópia extraída (acusa) e removido (silencia). **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | CI-1 | Baixa | 💡 Decisão tomada | — |
| BASELINE-FRAGIL | W7 | Testes | Teste de contrato `test_mapa_status_congelado_consumidor_b` comparava o **texto bruto** da célula de status da fixture real -- qualquer edição de prosa do dono do arquivo (fixture viva, editada por outra sessão) quebra o teste mesmo sem nenhuma reclassificação de verdade. 1ª rodada: baseline passou a congelar a **classificação derivada** (kind do emoji + predicados `is_pending`/`is_flip_eligible`/`is_awaiting_verification`/`is_done`), não o texto da célula. **2ª rodada (achado do team-lead):** isso não bastava -- a premissa de comparar contra o **arquivo vivo** a cada execução é frágil por desenho, porque outro projeto pode ser editado por outra sessão a qualquer momento, inclusive durante a suite (achado ao vivo: 2 itens do consumidor B perderam o emoji-prefixo reconhecido entre uma execução e a seguinte). Conserto: todos os testes de contrato passam a ler um **snapshot imutável** do texto da fixture (cache do pytest, nunca versionado, com metadados de origem/timestamp), congelado uma única vez e reusado até regeneração explícita -- nunca mais o arquivo vivo diretamente. Uma única variável de ambiente (`TAB_PENDENCIAS_REGEN_BASELINE_CONTR1`) regenera snapshot **e** baseline de classificação atomicamente (nunca só um dos dois, o que compararia classificação nova contra baseline velho). Provado por mutação com corpus sintético: edição do arquivo vivo (prosa ou reclassificação) não afeta mais nada após o snapshot congelado; mutação do **próprio parser** (predicado `is_done`) sobre o mesmo insumo congelado ainda acusa, nomeando o item. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | CONTR-1 | Baixa | 💡 Decisão tomada | — |
| LEAK-2 | W7 | Segurança | **Continuação do incidente `SANIT-1`**: 5 ocorrências de nome de projeto-fonte real, desta vez dentro do CONTEÚDO (comentário/docstring de teste), não do caminho -- vetor que a Camada 2 do guard não cobre (ela só checa caminho). Achadas e substituídas por designação neutra "consumidor A"/"consumidor B", sem perder o sentido técnico do comentário. Guard `guard_no_real_fixtures.py` ganhou **Camada 3** (conteúdo, opt-in, mesma fonte de termos das Camadas 2/3), com mascaramento do termo na própria saída (evita auto-vazamento) e aviso explícito quando as camadas opt-in estão desligadas (o "OK" antigo não distinguia proteção completa de proteção parcial). Provado por TDD com plantio/remoção de termo sintético em repositório descartável (exit 1 → exit 1→0). **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | SANIT-1 | Baixa | 💡 Decisão tomada | — |
| SKILL-AUDIT | W8 | Audit | Integrar `--audit` na `SKILL.md` (contrato, formato de saída, exit codes) e atualizar o `argument-hint` do frontmatter (D-7). **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | CHK-CORE, CHK-GRAPH, CHK-09, CHK-10 | Média | 💡 Decisão tomada | — |
| AC-REAL | W8 | Testes | Aceitação em corpus real, localizando **pelo conteúdo, não pelo número da linha**: **AC-1** consumidor B -- achar o `TODO-PARSER-BUG` invisível por pipe cru no próprio texto, o fragmento truncado de `ATOM-3`, e o sprawl da canônica; **AC-2** consumidor A -- reconciliar 116=116 com zero CRÍTICO (ou achados listados e explicados, nunca "passou" por omissão). **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | CHK-CORE, CHK-GRAPH, AUDIT-PATH | Média | 💡 Decisão tomada | — |
| CHK-CASA | W8 | Audit | Camada de **convenção da casa** (opt-in, desligada por default): **CHK-12** TST-*/AUD-* agendados antes do que cobrem; **CHK-13** INBOX (IDs duplicando a tabela, formato); **CHK-14** item fixo de Wiki+doc-iniciante como última onda. Fora do perfil "casa" nem aparecem no relatório -- é o teste vivo da separação núcleo × casa. **Pode escorregar para v1.1 se W8 apertar.** **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Baixa | CHK-CORE | Média | 💡 Decisão tomada | — |
| FIX-ENG | W9 | Fix | `tools/todo_fix.py`: aplica APENAS o mecanico e byte-preserving -- escapar pipe cru, remover fragmento truncado apos diff, consolidar tabelas fragmentadas (ncols identicos, preservando ID/Status/Estado Auditado, sem reordenar e sem recalcular WSJF), corrigir claim obsoleta. NUNCA muda status, reordena ou deleta branch/commit. Pre-condicao: working tree do TODO.md limpa; re-parse apos apply. Ao final de todo `--audit`, sugerir o `--fix`. **CANCELADO 2026-08-16 (legado / anti-OE):** funcoes e roadmap pre-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Nao reabrir sem novo item e escopo explicito do lider. | Alta | AUDIT-ENG, CHK-CORE | Alta | 💡 Decisão tomada | — |
| AC-FIX | W10 | Testes | Aceitação **AC-4**: `--fix` sobre uma **cópia** do corpus do consumidor B conserta os 2 defeitos vivos, mantém round-trip byte-exato no resto, e o `--audit` seguinte sai limpo desses checks. Gate obrigatório: o `--fix` reescreve arquivo do usuário e o erro aqui é irreversível do lado de fora. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | FIX-ENG | Média | 💡 Decisão tomada | — |
| SKILL-PATH | W10 | Docs | Conserto do caminho absoluto `~/.claude/githooks/` no `SKILL.md` (3 ocorrências: comando `todo_sync.py`, comando `todo_health.py`, e o README do toolkit), substituído por caminho relativo ao próprio repo (`tools/todo_sync.py`, `tools/todo_health.py`, `tools/README.md`) e pela referência normativa `references/frescor-da-tabela.md`. Achado por `DOC-FRESH` em 28/07, confirmado na fonte e corrigido nesta fatia. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | ABS-1 | Baixa | 💡 Decisão tomada | — |
| README-1 | W10 | Docs | README bilíngue corrigido para **9 colunas** (documentava 8 em `README.md:34-37` e no espelho en `:131-134`), com matriz de instalação, as duas camadas (núcleo genérico × convenções da casa), as dependências externas hoje não declaradas, e os modos degradados (sem agents → thread direta; sem vault → wikilinks mortos; sem python3 → só a parte agent-driven; Windows sem Git for Windows → perde só o sync mecânico via hook). **DoD inclui o check de consistência README × SKILL.md** -- o gate que teria pegado o schema obsoleto; conferido manualmente (schema de 9 colunas idêntico nos dois arquivos). Seções `--audit` (catálogo de 14 checks, core+casa) e `--fix` (escopo real de 2 classes, não as 4 do ADR; riscos conhecidos declarados) escritas nos dois espelhos, verificadas executando `tools/todo_audit.py` e `tools/todo_fix.py` de verdade. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | SKILL-AUDIT, FIX-ENG | Média | 💡 Decisão tomada | — |
| TST-T15 | W10 | Testes | Pre-CI local: `scripts/preci.sh` espelhando `.github/workflows/ci.yml` na mesma ordem (pytest, shellcheck + smoke dos shims, markdownlint-cli2, gitleaks, os dois guards; ruff como camada extra não-bloqueante), para o vermelho não aparecer só no servidor. Diferenças vs. o CI (matriz de 5 ambientes reduzida a 1, entre outras) declaradas na própria saída do script. Ver `TESTES.md`. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Média | CI-1, FIX-ENG | Baixa | 💡 Decisão tomada | — |
| AUD-FINAL | W11 | Auditoria | Auditoria adversarial **única** por `qa-engineer` independente (≠ implementer, ≠ orquestrador), consolidando o que em projeto maior seriam 7 auditorias: mutation testing de cada guard, cobertura significativa, meia-página de superfície de ameaça do hook (código de terceiro executando na máquina alheia), SAST + secrets, e **conformidade de licença pós-GPL-3**. Arquitetura não vira item aqui porque virou ADR-1. Entregue em `docs/auditoria/AUD-FINAL-2026-07-28.md`: 2 achados CRÍTICO (bypass de `--output` via symlink em `todo_audit.py`; nomes reais de projeto ainda no histórico git pós-`LEAK-2`, fix forward-only não reescreveu histórico) bloqueiam a tag `v1.0.0` até correção; 2 IMPORTANTE (mutante sobrevivente na prova de round-trip do `--fix`; CAMADAS 1/2 do guard anti-vazamento sem teste automatizado) e 5 COSMÉTICO viram follow-up. Ver `AUDITORIAS.md`. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | AC-REAL, AC-FIX, TST-T15 | Alta | 💡 Decisão tomada | — |
| SYMLINK-1 | W12 | Segurança | Conserto do achado CRÍTICO de `AUD-FINAL`: `_output_forbidden` (`tools/todo_audit.py`) comparava caminhos por `os.path.abspath` (só normaliza a string), que não resolve link simbólico -- um symlink criado fora do repositório mas apontando para dentro dele contornava a proibição de `--output` e o `--audit` (contrato read-only) escrevia de fato dentro do repositório auditado. Trocado por `os.path.realpath` em `_output_forbidden` e no gêmeo que alimenta a mesma fronteira (`--todo`, que calcula `root`); PoC reproduzido antes e depois, mutation testing em cópia extraída fora da árvore confirmou vermelho→verde. Demais ocorrências de `abspath` do módulo (`TOOLS_DIR`, `_check_run_file`, `checks/chk_frescor.py`, e as duas de `todo_fix.py`) avaliadas e mantidas -- não decidem fronteira contra caminho controlado por quem invoca. Hardlink e TOCTOU ficam declarados em aberto (fora do escopo desta fatia). **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | AUD-FINAL | Média | 💡 Decisão tomada | — |
| REL-1 | W13 | Release | CHANGELOG do zero (referenciando os consertos de 27-28/07 já feitos no toolkit, por descrição de conteúdo, não por SHA -- o histórico foi reescrito mais de uma vez por sanitização de vazamento, e hash antigo citado aqui já não seria estável) + tag **v1.0.0** + release no GitHub. **A tag exige confirmação do líder no contexto.** Entregue em `CHANGELOG.md`. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | AUD-FINAL, SYMLINK-1, README-1 | Baixa | 💡 Decisão tomada | — |
| MIG-1 | W14 | Migração | Migração da máquina do líder (D-8): `~/.claude/githooks` vira **symlink** para `tools/hooks/` do clone, e a skill instalada passa a apontar para o clone. Passo de verificação obrigatório (`git config --global --get core.hooksPath` + commit de teste em repo sacrificável). Não pode deixar os hooks mortos nem duplicados. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | REL-1 | Média | 💡 Decisão tomada | — |
| AUD-FUP-1 | W15 | Testes | **Follow-up IMPORTANTE da auditoria**: `_provar_invariantes` (`tools/todo_fix.py`), a rede que decide se e seguro escrever no arquivo do usuario, nao tem teste que prove deteccao de divergencia **so de espaco em branco ou terminador** numa linha preservada -- um mutante que enfraquece a comparacao byte-exata sobrevive a suite inteira. E o ponto cego justamente na classe CRLF/BOM, que o proprio projeto identificou como o Windows da promessa cross-platform. Entregue: 3 testes em `tests/test_todo_fix.py` (trailing space, CR residual de CRLF, e `_provar_e_escrever` sem tocar disco). | Média | AUD-FINAL | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| AUD-FUP-2 | W15 | Testes | **Follow-up IMPORTANTE da auditoria**: as camadas 1 e 2 do `guard_no_real_fixtures.py` funcionam hoje (comprovado ao vivo pela auditoria) mas **nao tem nenhum teste automatizado** -- so a camada 3 tem. A camada 1 e a unica ativa no CI publico sem configuracao alguma, ou seja, a que mais precisaria de rede de regressao. Entregue: testes de `check_table_sizes` + `check_forbidden_path_names` + main() em `tests/test_guard_no_real_fixtures.py`. | Média | AUD-FINAL | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| AUD-FUP-3 | W15 | Auditoria | **Follow-up COSMÉTICO da auditoria** (5 achados agrupados): contagem de skip na primeira rodada de cache frio; achados do `ruff` hoje declarados informativos sem config commitada que os torne gate; testes sem marca de licenca; dois `open()` sem encoding explicito no log de frescor; e a nota de que o proprio auditor escreveu nomes reais na primeira versao do relatorio antes de corrigir (nunca commitada). Entregue: nota de skip frio + ruff-nao-gate em `TESTES.md`; encoding utf-8 no log de frescor ja estava presente (nada a mudar); ruff.toml nao criado (sem gate); license so em testes novos se houver arquivo novo (esta fatia edita suites existentes). | Baixa | AUD-FINAL | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| WIKI-1 | W15 | Docs | Wiki do repo (GitHub wiki-native) + documentação `.md` extensa em registro didático para **iniciante em computação** (explica todo jargão, passo a passo, sem assumir conhecimento), derivada de `docs/` -- linka, não duplica. Regra da casa: último item, pós-tag, executado por `technical-writer`, **nunca inline**. Entregue: `docs/para-iniciantes.md` (comandos executados e verificados de verdade) + 5 páginas na Wiki nativa do GitHub (Home, Comandos, Auditoria-e-Correcao, Conceitos, Solucao-de-Problemas), publicadas via push no repositório separado da wiki. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Baixa | REL-1 | Alta | 💡 Decisão tomada | — |
| HOOKDOC-1 | W15 | Docs | Conserto da receita "por projeto" de `tools/README.md`, publicada quebrada na v1.0.0: o shim `post-commit` procura `todo_freshness.py` **um nível acima** de si mesmo, e `todo_freshness.py` importa `todo_lib.py` da **mesma pasta** que ele -- a receita antiga copiava os três arquivos juntos para dentro de `.githooks/`, quebrando as duas exigências. Corrigido preservando a estrutura de dois níveis do próprio `tools/` (`tools/hooks/` + `tools/`) dentro do consumidor: shims em `.githooks/hooks/`, os dois scripts Python em `.githooks/` (um nível acima). Os shims em si **não** foram alterados (caminho relativo correto para o layout do repositório). Verificado ao vivo com `git commit` de verdade num repositório descartável, tanto para a receita "por projeto" quanto para a "global" (nenhuma das duas tinha sido testada antes). Teste automatizado novo (`tests/test_install_recipe_e2e.py`, 3 casos) exercita as duas receitas via hook real, não só chamando o script Python direto. `docs/para-iniciantes.md` e a Wiki (`Solucao-de-Problemas`) atualizados para a receita corrigida, removendo o contorno documentado antes. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | — | Média | 💡 Decisão tomada | — |
| PRED-FALLBACK | W15 | Consertos | Reconsideração do fallback de tabela legada dos predicados de status (`tools/todo_lib.py`), pedida em um relato interno (não versionado) pela sessão de um consumidor (medição de 80 casos contra o toolkit anterior, 4 divergentes). Trocado o fallback "contém a palavra X?" por-predicado por uma única função de decisão (`_classify_legacy`) com **precedência por posição**: o vocabulário canônico de menor índice na célula vence, com os compostos (`pendente design`, `pendente verificação`) casados antes da regra de posição para não ressuscitar o BUG-5 grave. Resolve a incoerência `'Concluído e verificado'` (saía com `is_done` e `is_awaiting_verification` ambos `True`) e os 3 falsos-negativo medidos (`'Pendente (verificar disponibilidade)'`, `'Em andamento (verificar com o time)'`, `'A verificar'`). Camada de emoji intocada (prova por diff). Suíte antiga inteira (447 casos) preservada; asserção de `tests/test_bug5_predicados_emoji.py:58` trocada com comentário datado. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | BUG-5 | Baixa | 💡 Decisão tomada | — |
| TAB-SOT-007 | W16 | Distribuição | Detector de drift do pin de submódulo (`tools/submodule_pin_drift.py`), nascido de falha objetiva medida em 16/08: o gitlink de `skills/tab_pendencias` no `claude-memory` ficou **67 commits atrás** da revisão publicada sem que nada acusasse, e um `clone --recursive` restaurava a skill **sem o toolkit inteiro** (81 arquivos, 17.250 linhas). Read-only e warning-only: lê o gitlink por `git ls-tree HEAD` (não `git submodule status`, que inspeciona o checkout e falha se o submódulo nunca foi inicializado), consulta o remoto por `ls-remote` e **nunca** `fetch`, e **sem rede declara `nao_verificavel`, jamais `atualizado`**. Qualquer sinal de comparação falso vence sinal indeterminado, para drift confirmado não ser mascarado por incerteza. Ordena release por semver numérico e resolve tag anotada pela linha peeled. Caminho e URL são parâmetros. 24 testes em repos git fabricados. **Gêmeo CI entregue como snippet documentado e NÃO ligado**: ligar pertence ao `claude-memory`. | Alta | — | Média | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| PRED-FALLBACK-2 | W16 | Consertos | Defeito de integridade de dado na v1.0.2, achado por equivalência diferencial contra a cópia legada em `githooks.pre-v1.0.0`. O `is_flip_eligible` devolvia verdadeiro para `Pendente-design`, `Pendente depois design`, `andamento design` e `andamento depois design`, violando a regra dura D-1 de que item de design nunca flipa. Como o consumidor é o `todo_sync.py`, que **escreve** na tabela do usuário, o item seria promovido sozinho em silêncio. Causa-raiz: o `PRED-FALLBACK` implementou o composto como adjacência de string com radical fixo, e a forma adjacente exata passava por ser a única com teste. Trocado por **co-ocorrência restrita** de radical mais qualificador, sem adotar a prioridade incondicional do legado (que ressuscitaria a incoerência de `Concluído e verificado`). Gêmeos consertados em `Pendente verificação` e no radical `andamento`. Matriz de 216 combinações: 90 divergentes antes, 0 depois. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | PRED-FALLBACK | Média | 💡 Decisão tomada | — |
| TAB-ADD-001 | W17 | Intake | Núcleo mecânico de `--add` (`tools/todo_intake.py`): `WorkCandidate` + cascata ADR-0002 (d) + dedup por ID exato + persistência L0 (append puro com marcador `<!-- intake:id -->`) + residual INBOX com metadado de triagem + journal write-ahead + pré-condição de árvore limpa + abort se INBOX classificável (drain-first parcial). Apply L0/residual/DUPLICATE. Seção `--add` no corpo do `SKILL.md` (sem alterar `description` do frontmatter). | Alta | — | Alta | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-ADD-005 | W17 | Intake | SCOPED_REORDER apply: menor subgrafo seguro S, raw byte-a-byte fora de S, limite `scoped_reorder_max_fraction` (default 0.5 / env / ini), promoção a FULL se fração ou multi-Grupo; journal só após build ok. | Alta | TAB-ADD-001 | Alta | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-ADD-006 | W17 | Intake | FULL_REORDER apply: topo estável + ondas W1.., W1 Status intacto, marker intake; ciclo aborta sem escrita e sem journal órfão. | Alta | TAB-ADD-001 | Alta | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-ADD-007 | W17 | Intake | Strip residual INBOX do mesmo `item_id` após integração bem-sucedida (L0/SCOPED/FULL) e em DUPLICATE se residual coexistir com a tabela; P-dup mecânico só por ID exato na tabela canônica -- ID só na residual **não** é DUPLICATE, é strip+reentrada pós-decisão do líder; skill documenta re-chamada do intake com flags de julgamento e que 2–3 opções são dever do agente. | Alta | TAB-ADD-001 | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-ADD-002 | W17 | Intake | Dedup mecânico além de ID: descrição normalizada (strip, colapsar whitespace, casefold) contra tabela e INBOX residual; critérios de aceitação iguais se ambos tiverem o campo; sem NLP/semântica (fronteira agentiva). Testes mesma description→DUPLICATE / diferente→não. | Alta | TAB-ADD-001 | Média | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-INBOX-001 | W18 | INBOX | Migrar INBOX existente pelo pipeline de intake (classificar, integrar classificáveis, residual só com triage válido); n_antes = integrados + remanescentes + duplicatas fundidas. Orquestrador aplica drain no TODO real com tree limpa -- não nesta fatia de código. | Alta | TAB-ADD-001 | Média | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-INBOX-002 | W18 | INBOX | Circuit breaker mecânico em `todo_health`: emite `TAB_TRIAGE_REQUIRED` se classifiable>0 ou inbox_count>=3 ou residual cycles>=2; contagem separada de needs-leader-decision sem poluir o sinal. | Alta | TAB-INBOX-001 | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-INBOX-003 | W18 | INBOX | Invariante `classifiable_inbox_count == 0` após apply de `--drain` (e documentado para `--add`/`--reorder`); apply falha (rc=1) se classifiable restar. | Alta | TAB-INBOX-001 | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-INBOX-004 | W18 | INBOX | CLI `--drain` em `todo_intake.py`: dry-run lista; apply com `--judgments-json` (integrate/split/keep); residual válido só increments cycles; needs-leader não auto-integra; journal por candidato integrado; tree limpa no apply. | Alta | TAB-INBOX-001 | Alta | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-INBOX-005 | W18 | INBOX | `--drain` apply: intake **antes** de strip da linha INBOX (AUD-EXTREME Q14). `skip_classifiable_ids` no `run_intake`; strip da origem só após grupo integrate/split OK; se intake falha, TODO com a linha de origem intacta (sem perda, sem journal orfão de sucesso). Residual/keep só após intakes. | Alta | TAB-INBOX-004 | Alta | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| INTAKE-AGE-1 | W17 | Arquitetura | Predicado de envelhecimento residual (ADR-0002 hibrido): `todo_lib.residual_is_aged` = cycles>=triage_max_cycles **ou** idade>=triage_max_age_days (calendario avanca sem drain). Avaliado em `session_signals` + health + hook. Config `[signals]`. | Alta | — | Média | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-HOOK-001 | W21 | Hook | Hook SessionStart deterministico: mede fatos (TODO ausente, commits/dias, residual aged, inbox/, 🔍) via `session_signals`; sem LLM/WSJF. | Alta | INTAKE-AGE-1 | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-HOOK-002 | W21 | Hook | Sinais claros `TAB_*` (CREATE/SYNC/TRIAGE/CONCURRENT/LEADER_AGED/VERIF/RECOVERY); machine+human format. | Alta | TAB-HOOK-001 | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-HOOK-003 | W21 | Hook | Idade/ciclos exigem triagem, nunca full reorder por relogio (contrato em `references/sinais-de-frescor.md`). | Alta | TAB-HOOK-001 | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-HOOK-004 | W21 | Hook | Adapter `tools/hooks/tab_pendencias_reminder.py` so wiring (stdin JSON fail-open, exit 0); regra de negocio em `session_signals` (sem copia vault). | Alta | TAB-HOOK-001 | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| PYFLOOR-2 | W18 | Distribuição | **Decisão do líder em 16/08: subir o piso oficial de Python para 3.11** e passar a declará-lo. O piso anterior era promessa sem lastro: não declarado, sem teste, e a matriz de CI só exercitava versões acima dele. Declarar em metadado do pacote, documentar no README, e conferir que a matriz cobre o piso. A razão original de escolher config em INI era justamente evitar exigir 3.11+; o formato **fica como está** por decisão do orquestrador, porque trocar quebraria config de quem já usa e o ganho seria nulo. A justificativa do formato passa a ser histórica. | Alta | — | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| MDASH-2 | W18 | Governança | **Decisão do líder em 16/08: abrir exceção explícita do hook anti-travessão para este repositório**, com o motivo escrito. O travessão é o valor canônico de célula vazia no schema do produto, então o hook proibia o produto de mostrar literalmente o próprio contrato, e o README contornava descrevendo o símbolo em prosa. A exceção vale só aqui; a proteção segue nos demais projetos. | Média | — | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| HOOKSRC-1 | W18 | Distribuição | **Decisão do líder em 16/08: os ganchos vivos passam a rodar da instalação PUBLICADA**, não da árvore de desenvolvimento. Hoje `~/.claude/githooks` é symlink para o checkout de dev, então código não commitado executa a cada commit do líder em qualquer projeto da máquina. Apontar para o submódulo, que só carrega revisão publicada e testada. Custo aceito: mudança em gancho passa a exigir publicar e avançar o pin para valer. **PROVA 2026-08-16:** `~/.claude/githooks` retargeted para `~/.claude/skills/tab_pendencias/tools/hooks` (instalacao publicada, pin 5d42412); `core.hooksPath` permanece `~/.claude/githooks` e resolve para o skill, NAO para Projects/…/dev. md5 post-commit bate com produto; bash -n _chain.sh OK. | Alta | — | Média | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| VERB-STATUS-2 | W18 | Consertos | **Decisão do líder em 16/08: célula de status legada contendo só o verbo, sem vocábulo canônico, NÃO classifica -- e a auditoria passa a apontá-la.** Nunca agir errado, nunca ficar calada. Hoje o produto lê `Verificar disponibilidade` como aguardando verificação, e quem consome isso é a rotina que avança status sozinha na tabela do usuário. Decidir por análise de verbo assumiria português no conteúdo livre, o que o requisito canônico proíbe. | Alta | — | Média | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| SKILL-DESC-2 | W18 | Docs | **Decisão do líder em 16/08: escrever uma TERCEIRA redação** do resumo que aciona a skill. A reescrita parada na árvore encurta o texto e inverte o destaque entre os comandos; o pedido é ficar com o ganho (anunciar o comando de correção) sem o risco (mudar ordem de destaque sem medir efeito no acionamento). Fecha o `SKILL-DESC-1` e limpa a última alteração não publicada da árvore. Entregue: `description:` mantém ênfase histórica em `--create`/`--reorder` e anuncia `--audit`/`--fix`/`--add`/`--drain`; `argument-hint` alinhado. | Média | — | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| FIX-ESCOPO-2 | W18 | Docs | **Decisão do líder em 16/08: confirmada retroativamente a entrega de DUAS classes de correção automática**, e não as quatro do desenho original. As duas ausentes movem linhas, e mover linha em arquivo de terceiro é a operação de maior risco do produto. Ajustar ADR e README para descreverem o escopo real, não o pretendido. Regra que fica: a auditoria nunca marca defeito como auto-corrigível sem o corretor existir. | Média | — | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| MIG-DIFF | W16 | Distribuição | **Resolvido em 16/08.** Exigia provar que a versão do repo era superset comportamental da cópia legada antes de removê-la. Provado por equivalência diferencial executável: 816 comparações finais entre as duas versões, restando 2 divergências, ambas do fix intencional anterior onde o produto está certo. A cópia legada foi removida do vault por decisão do líder. Um achado da prova virou o `VERB-STATUS-2`. **CANCELADO 2026-08-16 (legado / anti-OE):** funções e roadmap pré-campanha deprecados pelo motor v1.2 (intake/drain/sinais/WSJF/lock). Não reabrir sem novo item e escopo explícito do líder. | Alta | — | Média | 💡 Decisão tomada | — |
| TAB-WSJF-001 | W19 | WSJF | Régua única Fibonacci `(1,2,3,5,8,13,20)` em `tools/wsjf.py` (`normalize_score` reject/snap); corrige a dupla régua 1-20 linear vs fib do `SKILL.md`/AGILE. | Alta | TAB-ADD-006 | Média | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-WSJF-002 | W19 | WSJF | Profiles `early` (default) e `safe` (SAFe formal): early aceita rótulos via `label_to_fib`; safe exige ints fib. Config INI `[wsjf]`. | Média | TAB-WSJF-001 | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-WSJF-003 | W19 | WSJF | Topologia antes de WSJF: `topology_before_wsjf` + `order_levels_then_wsjf` -- nivel-primeiro; WSJF só dentro do nivel; dependente nunca fura prereq. | Alta | TAB-WSJF-001 | Média | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-WSJF-004 | W19 | WSJF | Scoring local sem repriorizar o universo: peers só via `peer_scores` (nunca auto-mapear Prioridade da tabela); L0/SCOPED sem WSJF. | Alta | TAB-WSJF-001 | Média | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-WSJF-005 | W19 | WSJF | Estabilidade: `stable_rank_within_level` com epsilon comparável, empate estável, pin de `🔄` WIP, determinismo. | Alta | TAB-WSJF-003 | Média | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-WSJF-006 | W19 | WSJF | Explicabilidade: `explain_move` (3 linhas ITEM/causa/input_material) no report do FULL só para movimento material de pos/onda. | Média | TAB-WSJF-003 | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-WSJF-007 | W19 | WSJF | Criticidade factual: `source=bus` ignora rótulos retóricos; só ints explícitos pontuam. Integração FULL em `todo_intake.py`. | Alta | TAB-WSJF-001 | Média | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-CONC-001 | W20 | Concorrência | Um único escritor lógico do TODO por orquestração: subagente devolve `DISCOVERED_WORK`; main chama bridge+intake; workers não editam `TODO.md`. | Alta | TAB-ADD-001 | Média | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-CONC-002 | W20 | Concorrência | Sessões independentes: `tools/concurrent_inbox.py` grava `inbox/YYYYMMDD-HHMMSS-<session>-<slug>.md` (fallback, não backlog). | Alta | TAB-CONC-001 | Média | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-CONC-003 | W20 | Concorrência | Health emite `TAB_CONCURRENT_INBOX_PRESENT` + inclui `inbox/` em `TAB_TRIAGE_REQUIRED` para a próxima sessão principal drenar. | Alta | TAB-CONC-002 | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-CONC-004 | W20 | Concorrência | `TodoWriteLock` em `tools/todo_lock.py`; `run_intake`/`run_drain` apply adquirem lock (fcntl/msvcrt/exclusive+stale); timeout claro; testes de corrida. | Alta | TAB-ADD-001 | Alta | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| FIX-RISCO-A | W17 | Consertos | Mitigar corrida de dois --fix --apply: `run_fix` apply adquire `TodoWriteLock` antes de re-checar tree limpa e escrever; timeout rc=1. <!-- intake:drain-fix-risco-a --> | Alta | FIX-ENG | Media | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| FIX-RISCO-B | W17 | Consertos | TOCTOU RMW do --fix: lock de SO no apply + re-check de arvore limpa sob o lock (dry-run sem lock). <!-- intake:drain-fix-risco-b --> | Alta | FIX-ENG | Media | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| FIX-RISCO-C | W17 | Consertos | OSError generico em `os.replace` (mock PermissionError/OSError); path real Windows readonly nao exercitado no CI -- residual documentado em tools/README e comment. <!-- intake:drain-fix-risco-c --> | Media | FIX-ENG | Media | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-VAULT-001 | W22 | Vault | Fragmento `templates/vault/CLAUDE.md.fragment.md` com os 6 passos (descoberta->intake->L0/reorder/residual; TRIAGE acao da thread). SKILL.md corpo: INBOX e exception queue. | Alta | TAB-ADD-001 | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-VAULT-002 | W22 | Vault | Overlay `templates/vault/tabela-pendencias-frescor.overlay.md` aponta norma generica para a skill; nao duplica parser/CLI. | Media | TAB-VAULT-001 | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-VAULT-003 | W22 | Vault | Contrato `templates/agents/implementer-discovery-contract.md` (DISCOVERED_WORK); agents nao editam TODO/INBOX. | Alta | TAB-CONC-001 | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-VAULT-004 | W22 | Vault | Snippet `templates/vault/settings.sanitized.hook-snippet.json` com path relativo `skills/tab_pendencias/tools/hooks/tab_pendencias_reminder.py`; sem /home nem secrets. | Media | TAB-HOOK-004 | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-VAULT-005 | W22 | Vault | `scripts/recovery_drill.py` monta mock vault em dest, smoke do hook, assert path dentro do dest. Testes em `tests/test_recovery_drill.py`. | Alta | TAB-VAULT-004 | Media | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-BUS-001 | W22 | Bus | `tools/bus_contract.py`: BusMessage, extract_facts, candidate_from_bus (source=bus); claimed_priority/"urgente" nunca pontuam; time_criticality so int fib. | Alta | TAB-WSJF-007 | Media | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-BUS-002 | W22 | Bus | `archive_allowed`: mensagem so arquiva com rastro (item/dup/residual/leader). | Alta | TAB-BUS-001 | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-BUS-003 | W22 | Bus | Reference `references/bus-versus-inbox.md` (inbox do bus != INBOX do TODO). Corpus sintetico `tests/corpus/bus/`. | Media | TAB-BUS-001 | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-HUB-001 | W22 | Hub | Guarda `is_derived_hub` + `hub_is_derived_readonly` em run_intake/run_drain apply quando `[hub] derived=true`. Reference `references/hub-agregador.md`. | Alta | TAB-ADD-001 | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-HUB-GEN | W22 | Hub | Gerador deterministico do hub agregador. **CANCELADO 16/08 (anti-OE)** por ordem do lider: nao inventar gerador multi-projeto; produto fica com hub readonly guard + docs. Reabrir so com novo item e escopo explicitamente autorizado. | Baixa | TAB-HUB-001 | Media | 💡 Decisão tomada | — |
| TAB-CUT-001 | W23 | Cutover | Compat INBOX legada: linha sem `[triage ...]` valido e classifiable; dry-run de `--drain` emite `legacy_inbox_line`; sem exigir edicao manual de todos os TODOs. | Alta | TAB-INBOX-004 | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-CUT-002 | W23 | Cutover | Dogfood no proprio repo: `scripts/dogfood_metrics.py` + criterio `classifiable==0` (este repo ja drenou FIX-RISCO). | Alta | TAB-CUT-001 | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-CUT-003 | W23 | Cutover | Canary generico em perfis sinteticos (`project-small`, `project-large`); nao migrar todos de uma vez. Doc em `references/cutover-and-rollback.md`. | Media | TAB-CUT-002 | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-CUT-004 | W23 | Cutover | Metricas before/after (inbox_total, classifiable, oldest_age_days, n_items/n_verif/n_pending, sinais TAB_*; checklist operacional no reference). | Alta | TAB-CUT-002 | Media | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-CUT-005 | W23 | Cutover | Rollback: parar escrita auto, preservar journal/inbox/, captura conservadora, nunca apagar candidatos. | Alta | TAB-CUT-001 | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-TST-001 | W24 | Testes | Corpus sintético de contrato F10-01..26 em `tests/test_fase10_corpus.py` (rotas L0/FULL/SCOPED/triage/dup, topologia, WIP pin, residual aged, drain, hub derived, BOM/CRLF, 8 cols). | Alta | TAB-ADD-001, TAB-WSJF-003, TAB-INBOX-004 | Alta | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-TST-002 | W24 | Testes | Propriedades `no_lost_work`, `classifiable_zero_after_apply`, `topology_valid`, `wip_stable`, `sender_no_priority` em `tests/test_fase10_properties.py` (>=5 applies). | Alta | TAB-TST-001 | Media | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-TST-003 | W24 | Testes | Contract snapshots reais: reusa política CONTR-1/AC-REAL (fixtures locais imutáveis por hash; nunca versionar TODO vivo). Cobertura de regressão em `test_contract_real_corpus.py` + corpus sintético F10. | Media | CONTR-1 | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-TST-004 | W24 | Testes | Mutation adversarial em cópia `/var/tmp` (`tests/test_fase10_mutation.py`): topology identity, force L0 em foundation, residual_is_aged always False, BUS_SOURCES empty -- sonda vermelha; tools/ intocado. | Alta | TAB-TST-001 | Media | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-TST-005 | W24 | Testes | E2E instalação: copytree produto, recovery_drill smoke, session hook adapter, `--add`/`--drain` dry-run, `--audit` barato (`tests/test_fase10_e2e_install.py`). | Alta | TAB-VAULT-005, TAB-HOOK-004 | Media | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-SEC-001 | W24 | Segurança | Fronteira pública x privada: fixtures sintéticas consumer-a, guards stdlib + no-real-fixtures verdes, sem corpo de bus privado versionado (`tests/test_fase10_compat_sec.py`). | Alta | SANIT-1, LEAK-2 | Media | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-COMPAT-001 | W24 | Compat | 8/9 colunas legíveis, INBOX legada drenável, core offline (sem import de rede), guards CI OK (`tests/test_fase10_compat_sec.py`). | Alta | TAB-ADD-001, CI-1 | Media | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-REL-001 | W25 | Release | Fase 12: medir estado remoto antes de push (`fetch --all --prune`, `status`, `log -n 20`, `rev-parse HEAD` vs `origin/main`). Nunca presumir SHA do plano. Prep local: CHANGELOG 1.2.0 + SKILL-DESC-2 na árvore (sem tag/push nesta fatia). | Alta | TAB-TST-001, SKILL-DESC-2 | Baixa | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-REL-002 | W25 | Release | Fase 12: publicar `tab_pendencias` quando o líder autorizar -- push da revisão, conferir SHA por `git ls-remote`, observar CI real, release GitHub v1.2.0, testar instalação a partir do remoto (não do checkout local). Tag e push exigem confirmação no contexto. AGUARDANDO AUTORIZAÇÃO DO LÍDER para push/tag remota. Prep: HEAD local=origin/main medido; checklist em docs/auditoria/RELEASE-1.2.0-CHECKLIST.md **Push autorizado pelo lider 16/08** -- executado na sessao de fechamento. | Alta | TAB-REL-001 | Media | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-REL-003 | W25 | Release | Fase 12: **bump do pin de submódulo é do lado consumidor** (`claude-memory`): pin explícito no SHA validado de v1.2.0, `git diff --submodule`, commit separado, recovery drill. Este repo só publica a revisão; **não** atualiza o gitlink do consumidor. AGUARDANDO AUTORIZAÇÃO DO LÍDER. Pin é do consumidor claude-memory; bloco colável no checklist. NÃO editar ~/.claude sem ordem. **Pin no consumidor autorizado 16/08** -- aplicar no claude-memory. | Alta | TAB-REL-002 | Media | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-REL-004 | W25 | Release | Fase 12: prova de que o GitHub é fonte recuperável -- clone fresco `claude-memory --recursive` obtém skill+toolkit 1.2.0, hook, skill e um candidato de intake sem copiar arquivo de instalação antiga. Prova clone fresco pode rodar em /var/tmp quando autorizado; se falhar rede, reportar. Depende REL-003. **Clone fresco autorizado 16/08**. | Alta | TAB-REL-003 | Media | ✅ Concluído | TST+AUD 2026-08-16 (AUD-CAMPANHA-VERIF) |
| TAB-HOOK-005 | W26 | Hook | Roteamento por `hook_event_name` no adapter: o mesmo hook estava ligado a `SessionStart` **e** a `UserPromptSubmit` e emitia a MESMA mensagem de defasagem nos dois eventos, repetida ao usuário a cada turno. Particao exaustiva e disjunta em `session_signals.SIGNALS_BY_EVENT` (estado do repositório -> SessionStart; reativo ao turno -> UserPromptSubmit); campo ausente assume SessionStart (lado que nao repete), evento desconhecido nao emite; dedup por `session_id` so no evento por-turno (mesmas reasons nao repetem, reasons novas voltam a emitir), estado em `gettempdir()` com TTL + teto. 24 testes novos; mutation testing sobre o blob commitado. | Alta | TAB-HOOK-004 | Média | 🔍 Pendente verificação | — |
| TAB-LAYOUT-001 | W27 | Contrato | **INBOX ANTES da tabela** (LAYOUT-1): a ordem canônica do arquivo passa a ser título → preâmbulo → `## INBOX` → prosa opcional → TABELA → EOF. Motivo: com a INBOX depois da tabela, um guard de consumidor que exige "o arquivo termina na tabela" contradizia o produto por construção e bloqueava toda edição do `TODO.md`. Leitura do formato LEGADO (INBOX depois) continua VÁLIDA, com aviso (`todo_lib.legacy_layout_warning`); escrita e criação usam sempre a ordem nova. Novo `tools/todo_migrate_inbox.py` (`--check`/dry-run/`--apply`, idempotente, prova invariantes e escreve atômico) converte arquivo legado preservando tabela e INBOX byte a byte. Este `TODO.md` migrado com o próprio utilitário. 29 testes novos + mutation testing. | Alta | TAB-HOOK-005 | Média | 🔍 Pendente verificação | — |
| TAB-UNIQ-001 | W28 | Contrato | **Tabela única (de trabalho)**: o contrato não declarava quantas tabelas um `TODO.md` pode ter. Declarada em `references/frescor-da-tabela.md` §5 (+ `SKILL.md`, `README.md` pt+en) a regra "exatamente uma tabela markdown **de trabalho**" (= com coluna `Status`), com a qualificação que preserva tabela de **referência** sem `Status` no cabeçalho, mais a proibição de **linha em branco dentro da tabela**, e o motivo mecânico (a leitura para no 1º cabeçalho repetido; a linha em branco parte a tabela em duas na renderização). | Alta | TAB-LAYOUT-001 | Baixa | 🔍 Pendente verificação | — |
| TAB-UNIQ-002 | W28 | Auditoria | **`CHK-19` (tabela única)**: nenhum check contava quantas tabelas de trabalho o arquivo tem -- 3 arquivos reais acumularam 25, 13 e 3 tabelas sem um aviso sequer. Novo `todo_lib.table_blocks`/`work_tables` (bloco markdown com coluna `Status` **e** coluna de identificador) + `CHK-19` CRÍTICO no perfil `core`. Tabela de referência sem `Status` e legenda `\| Status \| Significado \|` (sem coluna de ID) NÃO são achado. Complementa o `CHK-03`, que só conta cabeçalho com a célula exata `id`. 22 testes novos. | Alta | TAB-UNIQ-001 | Média | 🔍 Pendente verificação | — |
| TAB-UNIQ-003 | W28 | Auditoria | **`CHK-20` (linha em branco dentro da tabela)**: não havia check para o mecanismo SILENCIOSO que parte uma tabela em duas -- em Markdown a linha em branco encerra a tabela, enquanto o parser atravessa e segue lendo (num arquivo real, uma linha em branco partia ao meio uma tabela de 308 linhas). Novo `todo_lib.blank_gaps_in_table` + `CHK-20` IMPORTANTE no perfil `core` (não CRÍTICO: com só a linha em branco nenhum item deixa de ser lido -- há teste que prova; o CRÍTICO é do `CHK-19`, quando o dado já sumiu). Buraco com heading (subtítulo, D-12) não é achado. 17 testes novos. | Alta | TAB-UNIQ-002 | Média | 🔍 Pendente verificação | — |
| TAB-UNIQ-004 | W28 | Migração | **Migrador recusa em vez de adivinhar**: com 2+ tabelas de trabalho, `tools/todo_migrate_inbox.py` elegia sempre a PRIMEIRA do arquivo -- elegeu a errada em 3 arquivos reais (num deles, uma tabela de 3 itens no lugar de uma de 339). Agora `plan()` devolve `ambiguous=True` + `work_tables=N`, `migrate_text()` levanta `MigrationError` e a CLI sai com 1 e "migração RECUSADA" SEM tocar no arquivo; com uma tabela só o comportamento é idêntico ao de antes. 11 testes novos (inclusive prova de que o arquivo não é tocado na recusa). | Alta | TAB-UNIQ-002 | Baixa | 🔍 Pendente verificação | — |
| TAB-UNIQ-005 | W28 | Contrato | **Forma do arquivo-modelo documentada** (§5.2 nova em `references/frescor-da-tabela.md`, resumo em `SKILL.md` e `README.md` pt+en): linha 1 declarando a estrutura antes do título `#`, heading próprio `## TABELA UNIFICADA` acima da tabela, e scoring WSJF/checklists em BULLETS no cabeçalho sob `### Scoring WSJF (referência — **não** é tabela de trabalho)` com a linha de escopo "o status de trabalho vive só na tabela única". Forma, não leitura: arquivo sem esses elementos continua válido e NENHUM check exige os títulos (agnóstico a projeto). 9 testes de contrato escrito. | Média | TAB-UNIQ-001 | Baixa | 🔍 Pendente verificação | — |
